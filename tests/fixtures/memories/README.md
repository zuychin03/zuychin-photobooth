# Synthetic then-and-now reference

`strip.png` is a 400 × 1000 flattened strip with four coloured panels and offset circles. It contains no people or capture metadata. Generate it with `node tests/fixtures/memories/generate.mjs` using the installed Sharp dependency. The normal browser file picker imports it locally to exercise crop selection, honest flattened-reference labels, unknown dates and reference persistence.
