# V2 development status

Updated 25/09/2026. [Approved scope](../../V2_PLAN.md). Release version: `2.0.0`. Development baseline: `main` at `6fd2387`. Danny authorised committing and pushing the limited friends release to `main`; full V2 acceptance remains separate.

Danny approved development and requires Astra for all subagents. Existing planning artefacts and 24 generated image sources are preserved. Package and lockfile declare `2.0.0`. The deployment scope is the local-only release described below, not activation of all hosted features.

## Limited friends release, 25/09/2026

Danny requested a shared incoming view so unfinished online features can stay unavailable during a friends-only deployment. `PB_RELEASE_MODE=local` now keeps the solo booth, editor, exports, device projects and templates available, while Together/rooms/relays, Events, cloud projects, Memories, accounts and the Shared Vault show **Feature incoming**. Production defaults to local mode unless explicitly set to `full`; development remains full unless local mode is selected. Proxy checks reject new online API work before account refresh, the client boundary covers navigation, and account initialisation/cloud-save controls are disabled. Existing receipt access/withdrawal, authenticated maintenance and locked-kiosk recovery retain their original checks; new kiosk capture/upload operations are refused.

Current main passed **1,147/1,147 tests**, nonincremental TypeScript and scoped ESLint. A final isolated production build passed with synthetic public Supabase settings, no service credentials and hosted switches disabled; all 689 copied source files matched main. Its **43/43 HTTP checks** covered working local pages, incoming rewrites including image-extension paths, refused APIs, hidden development routes and maintenance failing closed without credentials. Native production checks covered home, Together, Events, cloud projects, booth, device projects and templates, including both incoming action links, desktop and 320/375 px layouts without horizontal overflow. No console errors were recorded during these checks. Physical-device capture and OS delivery remain deferred.

The limited production preview is retained at `http://127.0.0.1:3011`; the main 3005 preview and full 3010 acceptance environment were not changed. Local tools require no Supabase setup or database migration. Existing hosted media still requires compatible schema/retention services; the release switch does not revoke older Supabase clients or replace database access rules. README and `.env.example` describe this boundary. No commit, push, deployment or hosted SQL was performed. This limited release does not close the full V2 acceptance queue below.

## Current acceptance queue, reconciled 25/09/2026

This queue supersedes older “next” and “remaining” statements below. It was reconciled against the conversation history, dated browser receipts, the retained service log, current source copies and the 1,139-test log. Historical checkpoints remain evidence for their stated scope; they are not instructions to repeat the work.

Latest user refinement: mobile navigation now floats 6 px from the side and bottom safe areas, with 16 px rounded corners, a full border and the retained 85% background. The corners were reduced from 24 px after a native comparison with the 16 px home actions; the latest 375 px rendered check, scoped ESLint, TypeScript and layout scan passed. This replaces the earlier full-width decision; the initial 12 px proposal was reduced at Danny's request. Its measured content reserve includes the floating bottom gap and becomes zero on desktop. Native 320/375 px inspection, exact 375/640 px insets, desktop hiding/zero reserve, scoped lint, TypeScript and the layout detector passed. The viewport was reset. `MobileNav.tsx` now matches in the main preview and the owned 3010 acceptance copy.

Genuine editable cloud acceptance now passes: the ordinary local-account UI created `Local editable design 25 Sep`, saved version 1 with eight verified originals, reopened an editable copy, changed its caption, and saved version 2. After reloading, version 2 reopened with `Cloud checkpoint two`; the previous checkpoint reopened with the original empty caption. Read-only service/database checks confirmed eight byte/hash/dimension-matching originals, identical bindings across versions, two distinct receipts and no duplicate uploads. Bao's project and all eight original reads were denied. The disposable deployment allowance alone was raised from 100,000,000 to 134,217,728 bytes after verifying capacity; no hosted budget changed. A resumed-save fix now reconciles stale upload tracking without resending bytes, with 26 tests, scoped lint and whole TypeScript passed. The fix, truthful creation-capacity copy, latest mobile navigation and reminder/export coordination are now synced to the owned 3010 copy.

Latest export follow-up: the first fix natively showed waiting instead of false empty, but automatic reminder/export reads cancelled one another through their shared busy guards. `EventReminderPanel.tsx` and `EventHostDetail.tsx` now sequence export loading after the first reminder read settles, including failure/unconfigured cases. The 36 existing reminder/export tests, scoped lint and TypeScript passed. After runtime sync, the genuine host page waited for host/reminder readiness, then automatically displayed its existing one-item snapshot without an export refresh or false cancellation. The reminder read's HTTP 503 is expected: the localhost launcher explicitly disables `PB_EVENT_REMINDERS_ENABLED`. This proves export loading through that unavailable-service boundary; it does not claim reminder delivery. Do not repeat the completed ZIP content check.

Already closed locally: P0–P8 feature implementation and their recorded synthetic workflows; template recovery and competing-save behaviour; dropdown modal fallback; the thirty-round media check; service-worker waiting/activation with a saved project; bounded local rollback; genuine local sign-in, cloud original upload/sharing/revocation and editable design version/checkpoint recovery; pairing/relay/vault/unpair and keep-drafts account isolation; concealed challenge reveal/withdrawal; event private photo delivery, private guestbook, gallery approval/hiding, withheld-wall viewing and ZIP contents. The detailed feature report was delivered in chat on 23/09 and 24/09; it is not an outstanding first-time deliverable.

| Still-open local boundary | Existing evidence to preserve | Next evidence needed |
| --- | --- | --- |
| Challenges and remote postcards | Genuine concealed two-person reveal and withdrawal; synthetic immediate/partial/group/postcard flows | Genuine immediate/partial consent and postcard handoff through the actual services. |
| Event lifecycle | Genuine receipt JPEG and browser-delivered ZIP on disk, private guestbook, gallery hide/private-delivery preservation, wall refusal, snapshot recovery, actual-service consent/admission matrix and private receipt delivery while paused | Private receipt delivery after closing the event still needs genuine Storage evidence. |
| Shared-device account boundary | Thirteen kiosk queue probes and native synthetic guest/recovery/PIN/exit flows; genuine keep-drafts sign-out | Genuine kiosk setup/sign-out/cookie/navigation lock and remove-local-copies sign-out. Physical camera/device turnover remains deferred. |
| Final integration | 1,139/1,139 tests, TypeScript and repository ESLint; fresh isolated production build with 77 routes and 31 static pages, targeted production HTTP/native checks; extensive dated route/component review, native export initial-load coordination and retained event/cloud detail selections after warm development refreshes | Authenticated production acceptance and any fixes from remaining checks. The cold development reset coincided with Fast Refresh; its precise cause and production relevance remain unproven. Reopen passing cases only for relevant source changes or new failures. |

The export initial-load fix in `EventExportPanel.tsx` waits for parent readiness and distinguishes waiting/loading from confirmed empty. Its 22 existing export tests, scoped ESLint and whole nonincremental TypeScript passed. The export, reminder coordination and moderation-focus files are now synced to 3010; native initial-load acceptance passed as recorded above.

The genuine event HTTP/API consent matrix passed on separate disposable event `892a8811-c1e0-4847-8593-9e5c77e8bd27`, now closed. Private/private, wall-only and gallery+wall submissions reached ready. All three refused destination approvals returned 403, and all three consented approvals returned 200. Fresh audience reads retained exactly one gallery item and two wall items across open, paused and closed states, with refused media grants denied. All three authorised private receipt reads passed while open; anonymous receipt media reads returned 403. Paused admission refused an existing guest's reservation with 410, and closed admission refused fresh invitation redemption with 403. Private receipt reads after pause/close were not repeated. The original event remains unchanged; this API evidence supplements existing native journeys. No source or capacity change was needed, workers ran only bounded passes, and the temporary helper was removed.

The browser ZIP delivery gap is now closed by fresh disk evidence. The expected `event-a99cb37a-6d05-4cee-b0ec-1bf2ec01eb51-export-727c5978-7a78-422a-be77-b950c3919aa1-g0-batch-000.zip` exists in Downloads, 63,937 bytes, SHA-256 `797b995a595b50927c78fa15fc1489fa22dd8c4af4f595b0ca812c2e7fbcea5c`. Its creation at 05:24:39 UTC on 25/09/2026 matches the recorded native download action at 05:24:38.391 UTC. All four existing archive entries pass Python's CRC check; the photo hash matches the genuine private JPEG, the guestbook matches the synthetic message, and both JSON reports parse. The earlier browser download-event timeout did not prove failed delivery. No archive was newly assembled or written to Downloads for this verification. Brief runtime-only diagnostics were restored byte-for-byte before any diagnostic download, and runtime source again matches main.

After verifying the existing ZIP on disk, the ordinary host UI reopened the same snapshot, restored its batch controls and accepted `I checked the saved ZIP`. The item now reads `confirmed saved`; releasing its temporary ZIP bytes, refreshing progress and reopening retained that status. No second browser download was needed.

Genuine paused-event private delivery now passes. The owner paused the original event through its confirmation flow; the guest refreshed the existing receipt, retained ready status and downloaded the photo. `Downloads/my-event-photo (2).jpg` was created at 07:05:20 UTC on 25/09/2026, contains 61,751 bytes and matches the original JPEG SHA-256 `5ceeeb07b5142de097f6c20ac8436eae348e5ef230eb6a97dcc5984c28fbcc09`. The owner then reopened contributions, and the host UI confirmed open status. No new upload or worker pass was used. Closed-event private delivery remains pending; the original event stays open for the remote postcard check.

The kiosk review found and fixed a discovery gap: owners now have `Set up kiosk` in their event dashboard, with a concise tooltip. It is unavailable during pending work or unsaved settings and hidden from non-owners and expired/deleted events. Native checks covered disabled/re-enabled behaviour after editing and restoring the event title, the tooltip at desktop and 375 px, no mobile overflow, and navigation to the genuine setup form. Setup no longer briefly labels an enabled deployment unavailable while its browser configuration loads. Reloading retained the expected setup form without the false unavailable message. No PIN was set, so the full genuine kiosk sign-out/cookie/lock journey remains open. Scoped ESLint, whole nonincremental TypeScript and both layout scans passed. Both changed components are synced to the owned 3010 copy.

The refreshed isolated production build passed after both kiosk changes: 77 routes, 31 static pages and build TypeScript. All 692 copied source files matched main and remained unchanged, manifest digest `418673fc10ca6f49b2f63108eff38f47762cb5d23134c3671f72148a6f7bc508`. The preceding targeted smoke covered five public pages, private kiosk headers, manifest, icons, service-worker revalidation and disabled events API behaviour; native home-to-Templates navigation loaded the empty shelf. After the final loading-state correction, the affected kiosk route and home passed again. Hosted flags stayed disabled. The owned 3011 production process was stopped; 3005/3010 were untouched. Build evidence is retained in `C:/Users/kduy1/AppData/Local/Temp/pb-production-check-AM8iVs/build-kiosk-fix.log`. Authenticated production services were not claimed from this disabled-service build.

The bounded authenticated detail follow-up reproduced an event return to its list while another route compiled. Browser console evidence showed Fast Refresh activity through 07:12:38 UTC on 25/09/2026. With compilation settled and no source edits, reopening the event and refreshing both event and cloud-project details retained their selected headings and returned both refresh controls to enabled. No further Fast Refresh entries appeared during that check. This is a warm-development refresh pass, not a proven root cause or forced token-refresh test. No speculative authentication change was made. Authenticated production acceptance remains separate because the retained genuine-service stack uses local development HTTP.

Immediate challenge `10b96dff-2497-414b-a49d-a726353e8581` is now open: Bao accepted in the ordinary browser UI, then Alex opened contributions. Four neutral app-icon originals are prepared for its two synthetic participants, but have not been uploaded. Automatic approval review rejected the upload/worker operation; an explicit user confirmation is pending for those four PNGs, the disposable app on port 3010 and Storage on 55431, and up to eight normal verification-worker passes including queued test cleanup. Do not retry or route around that pending approval. One allowed attempt exposed only a temporary helper import error, now fixed; authentication and challenge reads passed, and no upload IDs or worker mutations resulted. `.tmp-challenge-originals.ts` retains the prepared operation. Immediate visibility, partial consent and the genuine postcard handoff remain unverified.

Continuation is blocked pending user input. The same exact upload approval remains outstanding across successive goal turns, and independent local acceptance has now been exhausted. Kiosk credential entry requires user handoff in a dedicated browser profile; remove-local-copies sign-out needs approval for deleting the synthetic account's local drafts. Closed-event delivery follows the postcard check because closing the retained event cannot be reversed. Preserve the prepared helper, existing account/receipt tabs and running previews; do not repeat green builds or fixtures while waiting.

At the initial reconciliation, a read-only transaction found no retained editable designs, chapters, annotated memories, rituals or kiosk devices for the two synthetic accounts. The subsequent cloud and memory journeys above supersede that initial absence; it is not a reason to repeat them. The transient cloud connection recovered without restarting the stack or replaying migrations.

Genuine memory persistence passed: Alex opened an owned original, created `Local V2 memories`, and saved the occasion `Cloud keepsake check`. A browser reload retained both values; filtering by the chapter returned that memory and its photo opened again. A read-only database check confirmed the chapter at revision 0 and memory labels at revision 1, with Alex visible, Bao not visible and no matching Bao chapter or labels.

Genuine paired rituals also passed. The unpaired entry showed its partner requirement, then Alex and Bao paired through the ordinary local UI. Alex saved `Local weekly photo date`, weekly from 26/09/2026 at 19:00 Australia/Sydney (09:00 UTC). Pause was visible to both accounts; Alex resumed it and both reloaded the scheduled state. Only Alex had schedule controls. Alex's email preference persisted independently while Bao stayed opted out; a read-only database check confirmed the separate flags, and Alex then explicitly restored email off. Both accounts finish with email/push off. No sender or notification worker ran. This closes the genuine memory/ritual persistence boundary, while provider delivery remains an external gate.

Physical device/OS/print checks, hosted providers, cross-network/TURN, deployment load and different-release rollback, user cohorts and exact-authorised-commit CI remain separate release gates. Do not repeatedly run local fixtures as substitutes. No new document, commit, push, hosted change or deployment is needed to maintain this queue.

Latest UI refinement, 24/09/2026: the app now uses one transparent shared navbar with the two-colour `Zuychin / Photobooth` wordmark. Home uses Danny's final slogan and subslogan. Copy was shortened across capture, editing, libraries, Together, Memories and Events; optional explanations use a shared hover/focus/tap tooltip. Navigation guards preserve unfinished work and kiosk navigation stays locked. Native desktop/narrow checks covered ten ordinary routes, template leave/cancel focus, editor sizing and tooltip dismissal inside the export modal. Whole TypeScript, full ESLint (with the one shared-hook warning subsequently fixed and rechecked), and the final production build passed. See the latest receipt in [UI review](V2_UI_UX_REVIEW.md#shared-navigation-and-copy-refinement-24092026). Remaining P9 acceptance gates are unchanged; no commit, push or deployment occurred.

Mobile account follow-up, 24/09/2026: Danny replaced the separate Sign in row with a circular person icon at the top right. Signed-in navigation now uses a sign-out symbol and retains an explicit Vault link. The sign-out link opens the existing draft-choice confirmation through a consumed query parameter; it never signs out automatically. Genuine localhost password sign-in, confirmation focus, cancellation, reload without reopening, successful keep-drafts sign-out and the person icon's sign-in destination passed natively. Signed-in 320 px and signed-out 375 px layouts fit without horizontal overflow. Whole TypeScript, scoped ESLint and 28 focused relay/upload tests passed. Paired Unpair now requires confirmation, and the relay Back control is anchored below the shared navbar; those two deeper paired journeys still await native acceptance. This follow-up has not yet received a fresh production build.

Danny's latest menu refinement, 25/09/2026, keeps the pill on one row with horizontal scrolling and start/end chevrons only when its options overflow. It retains 16 px horizontal link padding, accent-pink hover and the transparent header. The two-colour wordmark uses the app's Fraunces font. Native 320/375 px and 1280 px checks passed, including both arrow endpoints, direct horizontal scrolling, resize recovery and keyboard access. Rendered testing caught a partially clipped focused option; focus now reveals its full bounds within the pill. Home, Together and the longer authenticated Vault menu were inspected. Whole TypeScript and scoped ESLint passed. The production Webpack build passed all 31 static pages using existing cached fonts after the default build could not reach Google Fonts.

Real paired relay acceptance, 25/09/2026: the retained disposable Auth/Storage stack was recovered without replaying migrations, and a fresh source copy runs on port 3010. Two genuine local accounts paired, uploaded four neutral icon images each, completed the relay, reopened it in the editor and saved a strip that decoded at 1072 × 3044 for both accounts. Unpair confirmation cancellation and focus passed; ending the synthetic pairing preserved the owner's strip, removed the former partner's refreshed access and made the relay deep link unavailable. The actual database has zero relay rows and one queued cleanup job; physical deletion was not exercised. Account drafts stayed hidden after sign-out and same-origin account switching, then reappeared for their original owner. A missing pairing-code accessible label was corrected and verified. See [the receipt](V2_UI_UX_REVIEW.md#real-paired-relay-and-account-isolation-25092026). The ordinary preview remains on port 3005. Real challenge, full event guest/moderation/export and kiosk journeys are the next local acceptance work; physical-device and external release gates remain open.

On 23/09/2026 Danny explicitly required a UI/UX check across every page and component before the goal can be marked complete. The release review must inventory routes and shared components, exercise desktop/narrow layouts and keyboard/touch alternatives, cover loading/empty/error/recovery states, and track findings to resolution. Read-only/static checks do not substitute for rendered review. Deferred real-phone checks remain separate and must not be represented as passed by viewport resizing.

On 23/09/2026 Danny removed Vietnamese support. The interface and authored prompts are English only; the language switcher and translation catalogue have been removed. User-authored Unicode captions remain supported.

## Home spacing follow-up, 25/09/2026

The later mobile-navigation refinement replaces the narrow pill with five fixed bottom destinations: Create, Together, Events, Projects and Templates. Every destination has an icon and label; Templates opens directly with no More menu. The desktop pill also pairs icons and labels, and only the pill stays 16 px from the top after passing its original position. The logo scrolls away. The top section spans the viewport with the brand left and pill right, using 20 px narrow and 32 px wider-screen gutters. Navigation uses stacking level 45 above page controls at 40 and below custom overlays at 50. Native booth hit testing reproduced content covering the bar before the fix, then confirmed all five labels above content afterwards; the real export dialog stayed above navigation and closing it restored Export focus. Danny reversed the temporary contrast increase, then requested a slight glass effect: the desktop pill uses 75% muted background and the mobile tray uses 85% background, both with 24 px blur and no added shadow. Measured bottom navigation height reserves page space and is subtracted from fixed-height editors/legacy rooms. Native 320/375 px and 768/1280 px checks covered active Templates, keyboard navigation, content clearance, breakpoint changes while scrolled, pin/unpin and editor Export/Share/save clearance. Whole nonincremental TypeScript, scoped ESLint and the layout detector passed. No new production build or physical-phone check was run.

Danny requested better spacing between the wordmark, navigation pill and home slogan. The shared header now uses a 16 px row gap and 20 px top padding; the home hero uses responsive top padding. Native measurements confirmed a 32 px pill-to-slogan gap at 320/375 px, 44 px at 768 px and 68 px at 1280 px, without horizontal page overflow. The transparent header, one-row scroller, wordmark and copy are preserved. Whole nonincremental TypeScript, scoped ESLint, the layout detector and diff checks passed. No production rebuild was run for this spacing-only change.

The navigation and spacing edits are synchronised to the main checkout/port 3005 and the retained 3010 acceptance copy. The synthetic event `Local event acceptance 25 Sep` has saved Celebration Garden and `Local V2 celebration`, with 16 MB storage and open contributions. The apparent invitation timeout was actually a strict-client rejection of a committed HTTP 201 response: PostgreSQL returned an offset timestamp instead of the required UTC Z format. Issue/session expiry values now normalise at the validated store boundary. The producer-to-client regression reproduced the fault before the fix; 21 focused tests and scoped ESLint passed afterwards. Retrying the preserved exact request natively now shows Invitation ready. Challenge acceptance below is complete within its stated local scope.

The real guest subsequently joined, uploaded one neutral app-icon photo and received a worker-verified private receipt. Native download produced a fully decoded 1072 x 1336 JPEG on disk. The host approved gallery publication, with wall approval disabled by the contributor's separate refusal; a newly created gallery link displayed the approved thumbnail. The guest saved a private message and signature, the host read both, and the gallery displayed neither. Hiding the gallery photo then cleared the audience image after a fresh permission check while refreshed private-delivery status stayed ready. Separate wall viewing showed no photos; display-mode entry and Escape recovery passed. An exact-retry export snapshot prepared and reopened successfully. Its genuine-service ZIP content check passed with one byte-identical photo, the private note, a valid manifest/CRCs and no failures; native ZIP delivery remains unresolved, and saved progress was not falsely confirmed. Remote postcards, all four genuine-service consent combinations and the remaining event lifecycle checks stay open.

Additional timestamp-boundary fixes passed 43 focused tests, scoped lint and whole TypeScript. A moderation cancellation-focus correction passed 18 existing tests, scoped lint, TypeScript and native Keep working/Escape checks in the main-checkout rehearsal. Development compilation/HMR reset some open pages without concurrent source edits; a logged 10.2-second successful host response exceeded the ten-second client timeout. Final authenticated acceptance still needs a stable runtime pass. See the precise scope in [the event receipt](V2_UI_UX_REVIEW.md#real-event-guest-and-navigation-follow-up-25092026).

The refreshed integrated checkpoint passed nonincremental TypeScript, repository-wide ESLint and **1,139/1,139 tests**, with zero failed, skipped or cancelled tests. The first run exposed an obsolete assertion expecting the logo in the home hero; it now checks the user-approved shared navigation placement while retaining the geometry, raster, manifest and asset checks. The updated brand file also passed its seven tests and scoped lint. No production rebuild, runtime sync, commit, push or deployment accompanied this checkpoint.

## Real concealed challenge acceptance, 25/09/2026

The ordinary two-account challenge journey now passes against genuine local Auth, Storage and the actual project worker on port 3010. The owner created a shared project, invited the second account, fixed an alternating two-person layout and opened contributions after acceptance. Each account explicitly uploaded two neutral app icons. One reservation expired during a prolonged browser-picker delay; Check status showed failure, and choosing/uploading a replacement recovered it. Verified originals were recoverable after a development remount, with duplicate assignment disabled.

After Alex submitted alone, server views exposed only Alex's own two originals and fresh cross-account reads returned 403. Bao's submission revealed all four originals to both accounts; every signed read returned byte-exact data. Both browsers decoded the shared 1072 × 3044 PNG, and the download action reported preparation. Bao's withdrawal preserved each person's originals but denied all four fresh cross-account reads again. Refreshing Alex cleared the open shared preview and full-result actions. See [the receipt](V2_UI_UX_REVIEW.md#real-concealed-challenge-lifecycle-25092026).

The full event guest/moderation/export journey and genuine kiosk isolation remain next. Earlier cloud workspace state resets coincided with development Fast Refresh activity, but causation is not established; a warm-route or production-runtime check remains needed. Native camera, physical devices, OS download delivery and external release gates remain unverified. No application source changed during this acceptance pass.

## Resume checkpoint, 24/09/2026

The ordinary two-tab designer check found and fixed a saved-copy URL defect. Initial saves and Save as a copy now navigate to their persisted template identity; reload preserves the intended copy instead of reopening the original. Conflict refusal, separate captions after both reloads and continued editability passed natively, plus 28 focused template tests. See [the competing-save receipt](V2_UI_UX_REVIEW.md#designer-competing-save-recovery-24092026).

Real localhost Auth/Storage acceptance passed its bounded core journeys using genuine Supabase services with migrations 001–028 and an isolated app copy on port 3010. Native magic-link sign-in and return paths passed on both loopback hostnames after correcting Next's reconstructed origin. Actual cloud upload, worker verification, signed previews, invitation acceptance, revocation and sign-out passed with two synthetic accounts. A separate real Storage check passed byte-exact owner access, anonymous/unrelated-user denial and cleanup. See [the native receipt](V2_UI_UX_REVIEW.md#real-localhost-authentication-and-cloud-originals-24092026).

Real Events HTTP acceptance also passed creation, guest redemption, signed upload, finalisation, ready receipt, decoded signed JPEG, access removal and confirmed derivative cleanup. This exposed and corrected Storage's HTTP 400 `NoSuchKey` response handling in event/project/voice adapters; arbitrary errors still fail closed. Cleanup correctly retains the outstanding staging-authorisation charge. Native event creation, saved scene/caption and unavailable-reminder guidance passed after correcting local export-client configuration. These are genuine local services, distinct from the earlier synthetic Auth/Storage schema fixtures. Physical devices, hosted providers, production TLS/cookies, scheduler load and cross-network rooms remain separate.

The integrated checkpoint passed **1,131/1,131 tests**, whole nonincremental TypeScript and full ESLint. After the Storage and export-client corrections, whole TypeScript, full ESLint and **65/65 focused tests** passed, including eight launcher and two disposable-stack preparation tests. The final reminder-copy correction passed scoped ESLint. The refreshed production build then passed, followed by **61/61 production HTTP checks**. No application source changed afterward. Rehearsal CI now also runs the disposable-service safeguards; the expanded `npm run test:rehearsal` passed **25/25**, including the additional receipt-only cleanup test.

The test app and all six owned Supabase containers, two disposable volumes and network were removed. Temporary credentials, scripts and runtime copies were purged; six sanitised readiness receipts remain in the owned temporary directory. The normal preview is restored at `http://127.0.0.1:3005/` with hosted V2 flags disabled and HTTP 200 confirmed. Git diff validation passed; HEAD remains `6fd2387`. No commit, push, deployment or hosted SQL was performed.

Danny confirmed there is no disposable staging environment and requested localhost testing for now. Continue isolated local rehearsals; real hosted authentication, Storage, provider and cross-subdomain checks remain pending. The feature report belongs in chat, with no new feature-guide document.

Latest recovery follow-up: the actual template row was extracted unchanged into a shared component and exercised in the existing development lab. Future/damaged records render recovery controls and the privacy warning without editing actions; deletion cancellation restores focus. The actual framework error boundary passed heading focus, keyboard Try again and healthy-page recovery at desktop/narrow sizes. See [the rendered receipt](V2_UI_UX_REVIEW.md#template-recovery-and-framework-error-follow-up-24092026). No new lab route or ordinary shelf data was added.

After that extraction, whole TypeScript and ESLint passed, followed by **1,122/1,122 tests**. The new `npm run test:rehearsal` passed **14/14** service-worker/artifact/proxy regressions and is now included in CI; it uses no hosted credentials or Docker. Windows required child-process permission for Node test workers. The package remains `2.0.0`; the unreleased changelog, README and activation checklist now reflect implemented features and separate project/event workers. `CHANGELOG.md` remains an ignored local record. Remote CI has not run.

The earlier production build after the template extraction passed. Its HTTP smoke passed **61/61** checks: all 17 lab routes return private 404, seven private page types preserve privacy headers, eight icon assets return 200, 25 disabled APIs return private 503, and home/booth/manifest/not-found checks pass. The ordinary production template shelf also resolved from loading to its expected empty state in the native browser. Later fixes are recorded above.

The new isolated legacy-room rehearsal connected both peers, recovered retained originals after storage/encoder refusal, reopened eight byte-exact originals per peer and reached both editor handoffs. Native review corrected mobile preview overlap and inaccessible desktop sidebar controls, then verified both fixes. See [the UI receipt](V2_UI_UX_REVIEW.md#legacy-room-follow-up-24092026).

A checksummed local fallback artifact is retained. The native local rehearsal switched from the current process to its frozen runtime and reopened four synthetic photos, the edited caption and Square/JPEG/80% export settings. It prepared an 18 KB JPG and exercised browser download; OS delivery remains unverified. Separate real-handler/disposable-PostgreSQL checks preserved a ready private receipt across admission pause and handler restart, refused new reservations, finalised accepted staged work and confirmed derivative cleanup while retaining staging charges. Eleven focused artifact/proxy tests, one actual SQL integration test and scoped ESLint passed. The rehearsal runtime and database were removed. See [the precise identity and limits](P9_REVIEW_QUEUE.md#retained-local-fallback-rehearsal-24092026). This uses one unchanged app build and synthetic provider bytes, not different-release compatibility or hosted rollback evidence. Hosted and physical-device acceptance remain pending.

The temporary production server was stopped after the rehearsal. The development preview is restored at `http://127.0.0.1:3005/`, with HTTP 200 and the actual Create/Together/Events landing page confirmed in a fresh browser tab. Hosted V2 flags remain disabled. Final `git diff --check` passed. No commit, push or deployment was performed.

The preceding integrated checkpoint passed **1,120 tests**, nonincremental TypeScript and full ESLint. A subsequent relay copy correction passed focused ESLint, and its clean production build passed. Windows refused an old generated build-directory cleanup; only the verified `.next` cache was removed after stopping this task's preview, then the build succeeded. That checkpoint's production HTTP smoke passed **60/60**: 16 actual lab 404s, seven private page-header checks, eight icon responses, 25 disabled/private API responses, and home/booth/manifest/not-found. The smoke's initial invalid room `status` action correctly returned 404; the corrected real `state` action returned the expected disabled 503. Strict UTF-8 decoding passed for 1,052 repository text files. Its temporary production server and worker proxy were stopped, and port 3005 was restored before the latest follow-up above. No hosted flag, database, deployment or Git history was changed.

New native evidence includes the thirty-round media session (300,224 ms, all rounds passed), natural A-to-B worker waiting/activation with an active saved project, the actual dropdown fallback branch in a native dialog, template rename/confirmation/dirty-leave focus, four bounded weekly recap checks, and interrupted relay checkpoint recovery. Relay capture now saves each original before advancing, restores a saved prefix and retains an unencoded canvas for retry. The fixture recovered one saved original after the second checkpoint was refused, then completed and reopened all four byte-exact originals with other-account denial. The relay fixture is a component/repository remount, not authenticated page reload or physical camera evidence. Recaps reject stale source/account output and stay within resource limits; photo-date failures remain visible and uncertain creation retries retain their request ID. See [P4 evidence](P4_EVIDENCE.md), [P9 receipts](P9_REVIEW_QUEUE.md) and [UI review](V2_UI_UX_REVIEW.md).

P9 remains open. Full authenticated legacy room/relay/vault journeys, the remaining UI state matrix, assistive technology, physical devices/printing/sharing, hosted services/TURN, user cohorts, deployment load/rollback and exact-commit CI are not established by these local checks. Do not mark the overall V2 goal complete.

### Earlier checkpoint, 23/09/2026

Danny explicitly resumed development on 23/09/2026. P0–P8 have local implementation and validation evidence. P9 source corrections and local review have advanced; full release acceptance remains open. English-only removal is implemented and rendered locally. Danny requested the feature report directly in chat, without a new feature guide document.

The latest integrated command passed **1,085 tests**, nonincremental TypeScript and full ESLint. The final production build passed after the last moderation-preview focus correction, which also passed scoped lint, TypeScript and native focus verification. A strict UTF-8 scan passed for 1,019 repository text files. Production smoke passed **58/58 checks** immediately before that isolated focus correction: all 14 lab routes returned actual HTTP 404, seven private page types had private/no-store/no-referrer/noindex headers, eight V2 icons returned 200, 25 disabled API/worker endpoints returned private 503, and home/booth/manifest/custom not-found checks passed. No hosted flags or budgets were enabled.

P8 native evidence now includes audience and host publication, guest permissions/guestbook, all 13 kiosk queue probes with successive guest/operator recovery, all eight postcard encoder/journal probes, a complete two-contributor exact-image approval and withdrawal flow, and six four-person room postcard preparation checks. P9 fixed auth return validation, legacy relay account lifetime handling, capture feedback preferences, modal dropdown fallback, project/postcard/moderation focus recovery, V2 navigation/recent projects, wall display mode, missing private headers and wall ID validation. Available route entry states and affected flows were rendered at desktop, narrow and landscape-tablet sizes; earlier stage receipts cover the deeper synthetic workflows. Authenticated hosted flows, physical hardware, installed PWA behaviour and the full release matrix remain explicitly unverified. Do not mark the overall V2 goal complete from this checkpoint.

Cloud design native validation now covers all 15 isolated journal checks and the actual save/open panels using synthetic transport: lost acknowledgement, workspace remount, exact-request reconciliation, editable-copy adoption, version 2, previous checkpoint, stale-head refusal, revoked original denial and account isolation. Narrow dark and desktop light recovery layouts were inspected. Review corrected confirmation action locking, duplicate project labels and singular file wording. Hosted authentication and Storage remain unverified.

Voice captions are wired from available memory rows behind the disabled server flag. Nine native draft checks and synthetic AudioWorklet recording, lost-acknowledgement save/delete retry, stale draft conflict, confirmation focus and access-loss clearing passed. Native accessibility clicks on the audio Play control crashed the embedded browser; an isolated one-second WAV played to completion using both an app button and a pointer click. Recorded PCM also played to completion via a pointer click. This distinguishes the observed automation path failure from the working playback paths without claiming physical audio output. Physical microphone, phones and hosted services remain unverified. The corrected and expanded 014 disposable SQL harness passed, including voice-only activity retention and missing-configuration quota refusal.

The bounded historical activity backfill CLI and runbook are source-ready, with eight injected transport tests and a zero-network help smoke passed. Async guided challenge stories now have migration 015, legacy-compatible contracts/drafts and UI; 75 focused tests and the disposable SQL suite passed. All 14 native draft checks and the guided two-person recovery/acceptance/assigned-camera walkthrough passed. The four-person walkthrough covered eight submitted originals, excluded-proposer lost-response recovery, separate contributor consent, partial reveal and cancellation. Three-person layout and older-server story fallback were also reviewed. P7 HTTP/session, image finalisation, signed provider endpoints, guest recovery journal and migration 017 host settings/discovery have focused and disposable SQL checks; host/guest/export UI integration is underway.

## Delivery register

| Part | Status | Remaining exit evidence |
| --- | --- | --- |
| P0 baseline and feasibility | Local implementation and checks passed; phone testing explicitly deferred | Real target-device feasibility remains pending; Danny authorised continuing local development |
| P1 secure storage and operations | Local implementation, tests and production build passed | Hosted Storage/provider behaviour, authenticated UI and deployment activation remain pending |
| P2 durable project and edit core | Local implementation and checks passed | Real camera/phone, hosted auth and OS download/share acceptance remain pending |
| P3 templates and visual packs | Local implementation and checks passed | Phone, real-person segmentation, assistive technology and hosted account acceptance remain pending |
| P4 print and solo motion | Local implementation and checks passed | Physical printing, real-phone codecs/memory, native share/download and installed-icon acceptance remain pending |
| P5 trustworthy live rooms | Local implementation and checks passed | Hosted activation, two/four physical devices, cross-network/TURN, real-person preview and mobile recovery remain pending |
| P6 rituals and memories | Local implementation and integrated checks passed | Final all-page regression; physical camera/audio, hosted authentication/provider and device checks remain separate |
| P7 event core | Local implementation, rendered rehearsals, disposable SQL, independent review and integrated checks passed | Final all-page regression, hosted schema/Storage/cookies, scheduler cadence/load, real notifications, QR scanning and OS downloads |
| P8 event experience | Local implementation, native rehearsals, disposable SQL and integrated checks passed | Real event setup/auth/cookies/Storage, QR scanning, actual device turnover and physical guest sessions |
| P9 accessibility, localisation and release | Source fixes, route/component inventory, local rendered checks, production build and HTTP smoke passed | Complete authenticated/device/UI state matrix, assistive technology, user cohorts, load/rollback evidence, exact-commit CI and authorised activation |

## P0 implementation

- Added `npm test` and `npm run check`, using pinned `tsx` to run Node's test runner against TypeScript modules. The CI workflow runs checks and a production build with read-only repository permissions and no deployment step.
- Capture/composition regression coverage uses recording canvas fixtures across all eight layouts. Browser pixel checks are a separate probe.
- Fixed `blobToCanvas` to release its decoded bitmap even when context creation or drawing fails. No capture or editor behaviour was otherwise redesigned.
- Added a development-only `/v2-lab` with synthetic storage, renderer, media and physical-geometry probes. Its production route is designed to return 404. No camera, microphone or cloud upload is requested by the probes.
- IndexedDB probes exercise atomic media/manifest storage, close/reopen recovery, injected transaction abort and additive index migration. A separate checkpoint workflow tests a real page reload. These are feasibility experiments, not the integrated C1 project library.
- Access and event-reservation models are isolated under `lib/feasibility/`, not connected to production RTC, API routes or database policies. See [access spike](P0_ACCESS_SPIKE.md).
- Media checks and their limits are documented in [media spike](P0_MEDIA_SPIKE.md).
- Added the bounded [resource inventory schema](P0_RESOURCE_BOUNDS.md), [workload decisions](P0_DECISIONS.md), known-surface memory calculations and actual desktop heap observations. These do not claim a safe decoder or total native/GPU memory measurement.

## Evidence

The previous P7 integrated checkpoint passed **984/984 tests**, whole TypeScript and full ESLint with a production build. It verified guest/receipt private headers, disabled event APIs, lab isolation and V2 icons before P8. The current 1,085-test/build checkpoint is recorded above. Synthetic fragment tokens were removed from browser URLs before unavailable-service feedback. Temporary production servers were stopped; development remains on port 3005. [Ritual UI](P6_RITUAL_UI.md) and [retained-strip reads](P6_RETAINED_STRIP_READS.md) distinguish native synthetic behaviour from unverified physical/provider acceptance.

The next P6 integration suite passed **671/671 tests**, whole TypeScript/ESLint and a production build. Three focused partial-fixture tests also passed after adding stale-roster recovery. [Partial-result UI](P6_PARTIAL_UI.md) passed native proposal/retry/consent/reveal, 1072 × 3044 PNG, excluded withdrawal and consent-reversal checks. The [direct challenge route](P6_CHALLENGE_ENTRY.md) passed local unavailable/invalid/recovery checks. Ritual HTTP and delivery passed focused and disposable SQL checks; the ritual UI and actual providers remain pending. Production smoke kept lab routes at 404, APIs disabled/private at 503 and all six V2 icons available with correct manifest entries. Camera and retained-read work now continue. Hosted activation, OS delivery and the final all-page/component review remain unfinished.

The previous P6 regression suite passed **626/626 tests**, whole TypeScript/ESLint and the production build. The primary challenge interface passed a two-account synthetic native-browser lifecycle: create, accept, open, upload, recover without completed local tracking, submit/retry, reveal, native PNG preview and withdrawal. Actual OS download delivery remains unverified. Partial-result projection/export and ritual schema/store passed focused tests and disposable PostgreSQL checks before their subsequent integration. Earlier cloud review covered sharing, deletion, recovery and eleven real IndexedDB/worker checks. See [challenge UI evidence](P6_CHALLENGE_UI.md) and [P6 evidence](P6_EVIDENCE.md).

The intermediate P6/security checkpoint passed **518/518 tests**, whole TypeScript/ESLint and the production build. Production smoke checks kept both lab routes at 404 and project endpoints disabled at 503, while revised V2 PWA icons served correctly. Story countdown/director progress and then-and-now import, crop, reload, next round, cancellation and GIF preparation were exercised in the native desktop browser. Cloud finalisation and reciprocal challenge rules passed isolated service tests and disposable PostgreSQL checks. P6 remains in progress; see [P6 evidence](P6_EVIDENCE.md) and [dependency maintenance](V2_DEPENDENCY_SECURITY.md).

P5 passed **411/411 tests**, whole TypeScript/ESLint and the production build. Native two/four-peer transfer, durable recovery, all-person preview, account cleanup and actual room UI rehearsals passed locally. The four-person failure rehearsal retained an unsaved original, repaired it without another shutter, preserved the incomplete round and completed a new 12/12 round. Desktop/narrow light/dark and keyboard review is recorded in [P5 evidence](P5_EVIDENCE.md). Both development lab routes return production 404; live room activation remains disabled. P6 and independent P7 work now proceed under the approved dependencies. This is not physical-device, hosted or cross-network acceptance.

P4 passed `npm run check` with **331/331 tests**, TypeScript and full ESLint, and the production build. Native browser checks passed five composition and five motion probes, including original preservation, bounded GIF cancellation/retry and actual MP4/WebM decode. The production editor prepared a 27 KB photo-loop GIF and 30 KB A4 PDF from synthetic photos. Revised PWA install icons are built and served. See [P4 evidence](P4_EVIDENCE.md). P5 remains in progress; this checkpoint does not satisfy the final all-page/component review or deferred device checks.

P3 passed `npm run check` with **284/284 tests**, TypeScript and full ESLint, plus the final production build. Actual browser evidence covers six template repository checks, four asset decoder checks, 72 compositor comparisons, 54 scene crops, normal save/import/reload/next-round workflows, keyboard focus, and production offline pack save/clear/fallback. Nine portrait focal corrections retain scene identity without changing source images. See [P3 evidence](P3_EVIDENCE.md). P4 and P5 now proceed in parallel under the approved dependency order.

P2 passed `npm run check` with **223/223 tests**, TypeScript and full ESLint, plus the production build. Actual desktop browser evidence includes ten repository checks, five native renderer/image checks, a real reload, the normal photo/project import flow, undo/redo and library operations. Danny's custom-dropdown request is included and was checked at desktop and narrow viewport sizes. See [P2 evidence](P2_EVIDENCE.md) for the exact limits, including unconfirmed OS download delivery.

P1 passed `npm run check` with **157/157 tests**, including TypeScript and full ESLint, and `npm run build`. The test runner required the same Windows subprocess permission used in P0; the initial sandbox run could not spawn test files. Actual local PostgreSQL and PostgREST concurrency/permission checks passed, including the complete consolidated setup and migration rerun. See [P1 evidence](P1_EVIDENCE.md), [database contract](P1_DATABASE_CONTRACT.md) and [cron contract](P1_CRON.md). The signed-out vault redirected to sign-in in the desktop browser; authenticated cloud UI remains unverified. This completes the local P1 foundation and permits P2 device-only work. It does not enable production cloud writes.

| Check | Result | Scope |
| --- | --- | --- |
| `npm run check` | Passed at the local P0 checkpoint | Repository typecheck, full ESLint and all 92 tests before P1 integration. |
| P0 test breakdown | 7 branding, 9 capture, 26 composition, 12 media/geometry, 2 memory arithmetic, 23 resource bounds, 12 access-model and 1 HTTP integration | Recording canvas fixtures and isolated models, plus actual loopback signalling requests. |
| `npm run build` | Passed | Production compilation, TypeScript and prerendering. Existing parent-lockfile root warning remains. |
| Desktop browser lab | Storage 3/3; reload recovery 1/1; media/geometry 13/13 | Actual IndexedDB transactions, page reload, canvas pixels and MP4/WebM encode/decode. Same-document reload verification correctly refused to pass. |
| Production route probe | `/` 200, `/booth` 200, `/v2-lab` 404 | Temporary local production server stopped after verification. |
| Remote CI | Workflow created, not run remotely | No commit or push was made. |

See [browser evidence](P0_BROWSER_EVIDENCE.md) for timestamps, measured bytes/durations, layout samples and limitations. The test runner and Next development/build commands initially required execution outside the sandbox's child-process restriction; no assertions were weakened to work around it. A green model test does not establish hosted database concurrency, and synthetic desktop media does not establish phone/camera performance.

## Known findings and remaining gates

1. P3 fixed the Together renderer's missing-participant case with whole-cell original-photo fallback when any intended cutout is absent. P5 still owns transport/readiness enforcement and true all-person live preview; local native compositor checks do not prove remote capture completeness.
2. P2 now persists capture time and timezone; native renderer checks confirmed stable stamps across wall-clock changes and the Sydney/UTC date boundary.
3. Real iPhone Safari (browser and installed PWA), Android Chrome and named desktop environments remain required. Desktop viewport resizing does not satisfy them.
4. Physical print dimensions/colour, actual camera quality, segmentation edges, memory pressure, quota exhaustion/eviction and long-session behaviour remain unverified.
5. The access prototype cannot certify shared Supabase RLS, server concurrency, production rate limits, TURN or cross-network WebRTC. Disposable database and actual deployment tests are required before enabling the corresponding features.
6. The 250 MB beta event budget includes outstanding two-hour upload authorisations and derivative headroom. The fixture permits 60 concurrent 4.1 MB reservations; 100 images is a count ceiling, not guaranteed available capacity. P0 chooses conservative prototype budgets; actual provider capacity remains a later activation measurement.

On 23/09/2026 Danny explicitly deferred phone testing and authorised continued local development. P0 mobile checks remain outstanding rather than passed. Physical printing belongs to P4 and hosted integration/load checks to their respective later stages, not an additional P0 permission gate.

No hosted SQL, production configuration, real-user media or sibling repository has been changed. No commit, push or deployment is authorised by this status record.
