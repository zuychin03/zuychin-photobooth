# P9 final review queue

Prepared 23/09/2026 from the approved plan, route/component source and recorded local receipts. This is a queue, not a passed P9 review. The dated follow-up receipts supersede pending entries in the original inventory where explicitly covered. The initial inventory itself did not perform browser, real account, provider, device or print checks.

For current work, use the [reconciled acceptance queue, 25/09/2026](V2_STATUS.md#current-acceptance-queue-reconciled-25092026). The original route/component tables below are historical coverage inventories, not a fresh instruction to rerun every passing workflow. The detailed feature report, synthetic cloud versions, kiosk queue, dropdown fallback, media session, worker update and local rollback already have receipts. Reopen a case only for a relevant source change, a new failure or the explicitly missing genuine-service boundary.

## Latest local follow-up, 24/09/2026

The [template recovery and framework error receipt](V2_UI_UX_REVIEW.md#template-recovery-and-framework-error-follow-up-24092026) now closes those two specific rendered gaps: actual read-only row controls, privacy warning, confirmation focus and the real framework retry flow passed desktop/narrow inspection. OS download delivery and assistive technology remain unverified. The existing lab route is reused; no additional route was introduced.

Release documentation now includes an unreleased V2 changelog entry, corrected package/status wording and separate project/event worker activation requirements. `CHANGELOG.md` remains ignored as a local record. `npm run test:rehearsal` runs 14 service-worker/artifact/proxy regressions, all passing locally, and is included in the CI workflow. The actual PostgreSQL rollback test remains separate because it requires its disposable Docker fixture. No remote CI run or hosted activation has occurred.

After the template extraction, whole TypeScript and ESLint, 1,122 tests and the production build pass. Final HTTP smoke passed 61/61 checks for 17 isolated labs, including `/v2-lab/legacy-room`; the ordinary production template shelf also rendered its expected empty state after loading. Legacy-room native two-peer storage/retry/finish/disconnect and mobile/desktop layout corrections are recorded in [the UI review](V2_UI_UX_REVIEW.md#legacy-room-follow-up-24092026). Danny requested localhost testing because no staging environment is available. The external release matrix remains open; the preceding retained fallback artifact and bounded local rehearsal are recorded below.

## Earlier local checkpoint, 23/09/2026

This later root receipt supersedes pending local items below where explicitly covered. The complete release matrix remains open.

- The integrated suite passed 1,085 tests, whole TypeScript and ESLint. Final production build passed, including the last moderation focus correction. The immediately preceding production HTTP smoke passed all 58 checks; only that isolated client focus change followed it. All 14 labs now return real private HTTP 404, seven private page types have full privacy headers, eight icon assets return 200 and 25 disabled API/worker endpoints return private 503. Unknown routes use the themed recovery page. A strict UTF-8 scan passed for 1,019 repository text files.
- Root rendered home, Together, booth, editor/export, projects, templates/designer, cloud library, memories/rituals, login/callback, legacy relay entry points, vault, challenge entry, V2/legacy room entry, event host/detail/join, receipt, gallery/wall, kiosk/locked recovery, postcard, offline and unknown-route entry states. Signed-out relay/vault routes returned to sign-in. Authenticated workflows were not invented from those entry checks; previous synthetic workflow receipts remain separate.
- Desktop 1366 × 768, narrow 390 × 844 and landscape 1024 × 768 were used where relevant. Home discovery now exposes Create/Together/Events and local libraries; recent projects render below the entry actions. A narrow hero clipping defect was corrected and rechecked (337-pixel copy container with retained side margins). Project deletion cancellation restored its exact trigger. Custom story selection passed ArrowDown/End/Enter with retained focus. Export dropdowns remained visible inside the modal, and closing restored Export focus. Capture sound/flash were initially off, and reduced motion explicitly suppressed flash.
- Wall display hid browse/report controls, retained an Exit/Escape action and cleared images when permission checks failed even with Keep current photos still enabled. First/Next photo navigation and Escape returned heading focus. Dark desktop and light landscape display were inspected. The valid wall entry route was rechecked after the UUID correction.
- Kiosk post-fix focus/turnover/revocation, guestbook/own-consent copy and focus, and complete two-person remote postcard recovery/approval/withdrawal are recorded in their existing P8 receipts. The postcard leave confirmation preserved edited choices on cancellation; recovery cancellation and successful forgetting restored meaningful focus. Final host preview cancellation restored its trigger, audience link creation focused the disclosed link and lost account authority removed the entire workspace and QR.
- Remaining: the full matrix in the tables below against actual authenticated/provider sessions, native fallback on a browser without Popover, assistive technology and mobile keyboard overlays, actual OS camera/mic/share/download/print/install/update behaviour, cross-network devices/TURN, long sessions/load/rollback and user cohorts. Real-phone work remains deferred at Danny's request. These limits prevent an overall completion or production-readiness claim.

## Priority findings

1. **Security, source confirmed:** Login consumes an unvalidated `next` in `router.push`; auth callback concatenates the same input after the origin. External/scheme destinations and `@host` callback targets need a shared strict internal-path validator. Repaired after this inventory with `safeAuthReturnPath` in both consumers; three focused malicious-target/query/hash tests passed. Rendered sign-in and callback/provider checks remain pending.
2. **Privacy, source confirmed:** Legacy `/relay/new` and `/relay/[id]` do not key/reset loaded state by account or cancel old load results. A different account can inherit stale couple/relay/checked state before a fresh read. Rechecked after the Voice agent repair: relay account-keyed state and asynchronous scope fences are now present. Voice owns the focused follow-up; final rendered account-switch behaviour remains separate evidence.
3. **Capture feedback repaired:** Fresh source inspection confirms `CaptureFeedbackSettings` is integrated into booth settings, `RoleCapture` and `LegacyRoom`. Audio reads the shared preference before playback; flash and countdown honour reduced motion. Sound and flash default off. Parent native review observed both choices off and successfully selected Reduce motion, with the text confirming flash was disabled. This supersedes the original queue finding. Real camera/device feedback remains part of final capture acceptance.
4. **Copy, source confirmed:** Root metadata and manifest still describe only “a booth for two”. Updated after this inventory to a concise solo/group/event description. Rendered/installed metadata refresh remains pending.
5. **Source corrections in this follow-up:** Missing-config/invalid auth callbacks now return to sign-in with the safe internal destination and private no-store/no-referrer headers. The fallback Dropdown portal stays inside its open native dialog, so modal inertness cannot disable the fallback listbox. Local project delete/recovery confirmations focus the non-destructive choice, cancellation restores the trigger, and completed mutations focus the stable library heading after refreshed rows unmount. Eleven focused callback/return-path/dropdown tests, scoped ESLint and whole TypeScript passed. Native modal fallback and project confirmation focus still need rendered confirmation.
6. **Approved landing/wall omissions repaired:** Home now lists at most three recent saved device/current-account projects using metadata only, with exact-scope resume and library recovery links. Account-keyed unmount and late-read checks discard old-owner results. The wall now has explicit display mode, an always-reachable Exit display button and Escape, plus a still-photo option. Reduced-motion preference holds the current group without stopping five-second permission checks or the ten-second freshness deadline. Fourteen focused recent-project/audience tests, whole TypeScript and scoped ESLint passed; rendered confirmation is pending.
7. **Evidence gap:** All old and new entry routes need one coherent final-build pass after the root kiosk guard, session, account cleanup and API changes. Prior feature checkpoints do not cover every resulting integration.

## Every production page and entry

“Recorded” below means the linked document reports rendered/native work. It is not a fresh result from this audit. Every row still requires applicable final desktop/narrow, keyboard, light/dark, loading/error/retry and English copy review.

| Current route | Recorded local evidence | Remaining final review |
| --- | --- | --- |
| `/` | P2/P3 production smoke; home appears in [master register](V2_UI_UX_REVIEW.md) | All three track entry points, newly added recent projects and their account/no-env states, new shared guard |
| `/together` | Source entry exists with capability-aware rooms/cloud/challenges | Dedicated rendered receipt not found in inspected register; discoverability, disabled explanations, navigation |
| `/booth` | [P2](P2_EVIDENCE.md), [P3](P3_EVIDENCE.md), [P4](P4_EVIDENCE.md), [P6](P6_EVIDENCE.md): imports, settings, story, reference, next round | Real camera permission/denial/unplug, real-device feedback, pending-save recovery, all new tool interactions |
| `/customize` | P2 editor recovery; P3 templates/assets; P4 export; P6 reference | Nine layouts, history/resource boundaries, current cloud-save navigation, all output dialogs and cancelled work |
| `/projects` | P2 rename/duplicate/import/reload; P5 housekeeping | Delete confirmations, damaged/future record backups, storage full/persistence, account cleanup and metadata housekeeping |
| `/projects/cloud` | [cloud journal/save](P6_CLOUD_SAVE_JOURNAL.md), master register: synthetic two-account originals/design/member workflows | Actual auth/provider gate; complete final English/narrow pass across recovery and design panels |
| `/templates` | P3 native import/new project and portable privacy | Complete rename/duplicate/delete/future-record/error flow on final build |
| `/templates/design` | P3 native design/import, labelled inputs, dropdown/keyboard | All layer tools, cancellation/dirty leave/CAS/resource failure, keyboard overlay and long labels |
| `/challenges/[id]` | [challenge UI](P6_CHALLENGE_UI.md), [partials](P6_PARTIAL_UI.md), [group partial](P6_GROUP_PARTIAL_REHEARSAL.md), [async story](P6_ASYNC_STORY_UI.md), [camera](P6_CHALLENGE_CAMERA.md), [drafts](P6_CHALLENGE_DRAFTS.md) | Direct-route sign-in/return and invited access on final build; actual camera; new postcard handoff; hosted reload remains distinct from fixture remount |
| `/room/[code]?v=2` | [P5](P5_EVIDENCE.md): native two/four-peer synthetic room capture, reconnect, saved-copy recovery; P6 guided story | New postcard handoff, final complete UI; separate-network/forced TURN, real people segmentation and physical devices |
| `/room/[code]` without `v=2` | Legacy branch remains active; P2 adapter/source compatibility | Dedicated full legacy rendered regression, scope changes, camera/finish/disconnect; do not substitute v2 fixture evidence |
| `/relay/new` | P1 upload-intent and P2 persistence source/tests | Legacy full author flow, account-switch fence, camera failure, retained upload retry, copy/share focus |
| `/relay/[id]` | P1/P2 source/test coverage; legacy deep link retained | Legacy pending/complete/expired/denied/retry/delete/editor handoff; account-switch fence |
| `/timeline` | Master register identifies vault/sign-out; source has owner/generation fence | Pair/join/unpair, weekly quota/keep/archive/delete, legacy recap/date/reminder forms, push, two sign-out choices and cleanup errors |
| `/memories` | [memory UI](P6_MEMORY_UI.md): chronological pages, private chapters, source states, annual selection, revoke/unpair, light/dark | Current voice integration, empty/provider-error states, final keyboard/copy; real retained object delivery |
| `/memories/rituals` | [ritual UI](P6_RITUAL_UI.md): synthetic lost ack/CAS/legacy/DST/member channels/access loss | Real notification permissions/delivery, final cross-navigation and current account runtime |
| `/login` and `/auth/callback` | Source and earlier unavailable-route smoke | Return-target repair, no-env and callback errors, legitimate query/hash preservation, actual authenticated flow only with authority |
| `/events` | [host UI](P7_EVENT_HOST_UI.md), [host publication](P8_HOST_PUBLICATION_UI.md) | Final discovery/create/capacity/invitation/empty/error flow; real worker/provider gate |
| `/events/[id]` | P7 host/test/private review/reminder/export; P8 moderation and links | Complete one-event host lifecycle after all P8 additions, pending actions/confirmation focus and export/expiry |
| `/events/[id]/join` | [guest UI](P7_EVENT_GUEST_UI.md), parent P8 guest work underway | Final guestbook/missions/destination-consent/replacement focus checks; no-account success against authorised environment |
| `/receipt/[submissionId]?event=…` | P7 guest/receipt narrow render; fragments scrubbed in local production smoke | All consent/withdrawal/note states after remount, wrong/expired token, final downloadable media authority |
| `/e/[slug]/gallery` | [audience](P8_AUDIENCE_BROWSER.md): six native probes, gallery/report/download and focus corrections | Final current authority/visibility/offline/expiry pass and physical-device download |
| `/e/[slug]/wall` | Same audience receipt: independent wall access, rotation, light desktop | New display-mode exit/Escape and still-photo controls, unattended freshness, final wide/tablet presentation |
| `/e/[slug]/kiosk` | [kiosk](P8_KIOSK.md): source/SQL/focused tests plus parent native synthetic workflow | Parent native queue 13/13 and successive-guest/lost-ack/failed-upload/operator-retry/exit workflow passed; focus corrections await rendered confirmation. Production guard/sign-out, idle/hidden and real cookie seams remain separate |
| `/kiosk-locked` | Source-only fail-closed recovery page | Render malformed/lost lock, no private page flash, back/BFcache and protected exit |
| `/e/[slug]/postcard` | [postcard contract](P8_POSTCARD_CONTRACT.md); new rehearsal ready | Root current E6 native work, all contributors/candidate approval/refusal/withdrawal, authority loss and explicit local recovery dismissal |
| `/offline.html`, unknown route and framework error/not-found views | P3 real offline curated asset check; [icons](PWA_ICON_V2.md) | Final offline visited shell after new guard, no private snapshot fallback, update with active draft/room; meaningful not-found recovery. Custom `app/not-found.tsx` and `app/error.tsx` are implemented; final exception-path recovery remains separate |

The approved target list also names `/events/new` and `/events/[id]/manage`; source currently embeds creation/management at `/events` and `/events/[id]`. This is a route mapping difference to document/resolve, not evidence of missing creation/management functionality. No standalone settings route exists; review settings where they actually live.

## Gallery legacy compatibility receipt, 24/09/2026

Read-only review verified the sibling checkout at `C:/Users/kduy1/OneDrive/Desktop/Projects/zuychin-gallery`, branch `main`, origin `https://github.com/zuychin03/zuychin-gallery.git`, commit `cc2e1af803b68f54f20825c88ff9185248abbf51`. It was clean before and after review. No sibling files, credentials or hosted resources were changed or inspected for secret values.

Structural compatibility is preserved: Gallery's `app/photobooth/page.tsx:31–47` reads `id`, `owner`, `caption`, `cloudinary_public_id` and `created_at`; Photobooth's baseline `supabase-setup.sql:79–99` retains those fields and migrations 001–028 do not replace the legacy SELECT policy. Gallery's authenticated-image delivery matches `lib/cloudinary.ts:29–43` in Photobooth. New project/event scopes are not exposed through this legacy reader.

This is a qualified compatibility receipt, not equivalent privacy behaviour across both apps:

- Gallery filters only non-null archive IDs, without `archive_verified_at` or a current provider check. Photobooth `lib/server/media-worker.ts:137–145` persists the archive reference before verification, so Gallery can show an archive-pending or unavailable entry. Original preservation remains the worker's responsibility.
- Gallery `lib/cloudinary.ts:64–75` creates signed CDN URLs without an explicit expiry, and `app/photobooth/PhotoboothClient.tsx:224–275` renders/downloads them directly. Existing URLs and open pages do not receive the retained resolver's fresh authorisation checks. Photobooth's `lib/server/retained-strip-objects.ts:28–43` instead checks provider metadata and uses a 30-second private download request.
- Baseline `supabase-setup.sql:217–220` retains owner-or-current-partner visibility, including historical legacy strips after re-pairing. This is the deliberately preserved broad legacy contract noted in `V2_PLAN.md:192`, not the exact-original-couple boundary enforced by `database/migrations/011_v2_retained_strip_reads.sql:6–25`. Do not describe Gallery's legacy archive as inheriting the newer memory/privacy guarantees.

Both apps' `lib/supabase/cookie-options.ts`, browser/server clients, callback and proxy use the same optional `NEXT_PUBLIC_COOKIE_DOMAIN`. The installed SSR implementations (Gallery 0.8, Photobooth 0.12) use compatible PKCE/base64url chunking and cookie defaults: path `/`, SameSite Lax, non-HttpOnly, 400-day lifetime. No cookie-format mismatch was found. This does not verify actual configuration, token-refresh races or cross-subdomain sign-out. Cookie sharing must remain limited to the explicitly configured shared identity project and parent domain; no UsTime integration is implied.

Danny's current acceptance environment is localhost; no separate staging environment is required or claimed. Real account authentication, configured Supabase/Cloudinary identity, callback allowlists, archive delivery/transforms and cross-subdomain cookie refresh/logout remain pending authorised checks. Local source inspection and disposable database evidence cannot certify those behaviours. Gallery was not changed to close the preserved limitations.

## Shared component coverage

This groups the components inventoried at preparation plus the subsequently added capture-feedback control. Component-local helpers and inline route forms inherit their parent row above; nonvisual modules require visible failure/lifecycle evidence. Presence in a group is not a rendered pass.

| Components | Evidence family and remaining review |
| --- | --- |
| `Logo`, `StripMockup`, `RecentProjects` | Home/PWA raster evidence; final home and inherited theme/size consumers |
| `CameraPreview`, `CaptureSettings`, `CaptureFeedbackSettings`, `Countdown`, `FilterBar`, `RoleCapture` | P2/P3 capture; explicit motion/sound/flash controls are now integrated and parent-rendered; remaining legacy/device capture acceptance stays separate |
| `Dropdown`, `PhotoEditPanel`, `ProjectLibraryRow`, `TemplateSourcePanel`, `VisualPackPicker` | P2/P3 rendered keyboard/viewport/focus evidence; recheck new consumers and final full controls |
| `ExportStudio`, `MotionExportPanel`, `SoloMotionStudio`, `ThenNowStudio`, `StoryGuide` | P4/P6 evidence; real output/device and final combined editor state |
| `AuthCookieMigration`, `InstallPrompt`, `PushToggle`, `PwaRegister`, `RoomMetadataHousekeeping` | Source/tests and partial parent routes; actual install/permissions/update/account-cleanup paths remain explicit queue items |
| `CloudControls`, `CloudLibrary`, `CloudMembers`, `CloudProjectDetail`, `CloudProjectHub`, `CloudUploadRecovery`, `CloudDesignPanel`, `CloudDesignSavePanel` | Cloud fixture rendered evidence; final real entry/runtime and complete state matrix |
| `ChallengeCamera`, `ChallengeCreate`, `ChallengeDetail`, `ChallengeDraftRecovery`, `ChallengeEntry`, `ChallengePartials`, `ChallengeStory`, `ChallengeWorkspace` | P6 challenge/partial/story/draft/camera receipts; new postcard integration, actual camera and final account lifecycle |
| `MemoryHub`, `MemoryLabels`, `MemoryRecap`, `MemoryWorkspace`, `RitualHub`, `RitualWorkspace`, `VoiceCaption`, `VoiceMemoryCaption`, `VoiceMemoryEntry` | Memory/ritual/voice receipts; final composition, authentic source/voice delivery, microphone/notification permission |
| `LegacyRoom`, `RoomEntry`, `RoomRoundPreview`, `RoomWorkspace`, `RoomPostcard` | P5/P6 rooms; LegacyRoom and newly added RoomPostcard need explicit final rendered coverage |
| `EventHostControls`, `EventHostEntry`, `EventHostWorkspace`, `EventHostDetail`, `EventHostForm`, `EventInvitationQr`, `EventHostTestPreview`, `EventHostContributionPreview`, `EventExportPanel`, `EventReminderPanel` | P7 host/export/private-preview/reminder receipts; complete P8-integrated host review |
| `EventGuestEntry`, `EventGuestWorkspace`, `EventGuestReceipt`, `EventGuestbookPanel`, `EventGuestConsentPanel` | P7 guest plus parent current P8 review; final receipt/consent/mission notes and replacement teardown |
| `EventAudienceEntry`, `EventAudienceWorkspace`, `EventAudienceLinks`, `EventModerationPanel`, `EventModerationPhoto`, `EventHostGuestbookPanel` | Recorded P8 audience/publication review; final event journey and narrow/keyboard/freshness regression |
| `EventKioskEntry`, `EventKioskGuard`, `EventKioskOperator`, `EventKioskWorkspace` | Parent native kiosk workflow and 13 real-IDB checks passed; post-correction focus and shared guard effects across every page remain |
| `EventPostcardComposer`, `EventPostcardEntry`, `EventPostcardPanel` | Source/tests and current parent rehearsal; final candidate/collaborator/recovery/privacy workflow |

Additional route-owned UI includes `TogetherEntry` and all app-page inline forms; do not omit them simply because they are not under `components/`. `components/cloud/challenge-story-review.ts` and `components/events/useEventHostTask.ts` are nonvisual helpers whose outcomes are covered through their consumers.

## Development-only route inventory

All current lab page files have an explicit development-only guard in source. The 24/09/2026 final local HTTP smoke verified actual 404 for all 17 routes below, with a production proxy guard preserving kiosk-lock precedence:

`/v2-lab`, `/v2-lab/media-acceptance`, `/v2-lab/relay-recovery`, `/v2-lab/legacy-room`, `/v2-lab/cloud`, `/v2-lab/room`, `/v2-lab/memories`, `/v2-lab/rituals`, `/v2-lab/voice`, `/v2-lab/voice/pcm`, `/v2-lab/voice/playback`, `/v2-lab/events`, `/v2-lab/events/host`, `/v2-lab/events/export`, `/v2-lab/events/audience`, `/v2-lab/events/kiosk`, `/v2-lab/events/postcards`.

Fixture evidence uses actual local UI/IDB/native codecs where recorded, with synthetic identities/transports. It never substitutes for real account/provider authorisation, genuine page/server persistence when the fake server resets, real phone hardware or human segmentation quality.

## Copy, gates and PWA source checks

- A targeted source scan over `app`, `components`, `lib` and `public` found no Vietnamese labels, Vietnamese-specific letters, `vi-VN`, `"vi"` locale or Vietnamese language-switch copy. Root and offline HTML use `lang="en"`. This does not certify every English phrase; preserve arbitrary Unicode user captions.
- `.env.example` keeps events, cloud projects, challenges, room v2, memories and voice flags explicitly false. Route/source capability gates were inspected; no local secret configuration was printed or interpreted as deployment state. Actual disabled APIs and private headers must be retested from the final build.
- Manifest/root metadata references revisioned V2 standard, maskable, Apple and favicon assets. The icon receipt records raster safe-circle checks and local production HTTP; installed iOS/Android refresh remains pending. Old aliases remain for compatibility.
- SW source has no forced `skipWaiting`, preserves static rollback assets and restricts cached public shells to `/`, `/booth`, `/customize`, `/projects` without queries. Private navigations use network/offline fallback. The new kiosk guard means offline/back/update behaviour needs fresh integrated evidence, not just the old P3 cache receipt.
- Package now declares `2.0.0`; this source version is not release or activation evidence. `supabase-setup.sql` remains the baseline plus P1 marker, while later foundations live in numbered migrations. The setup header now directs migrations 002–028 after the baseline. Final docs/release notes, exact migration order and rollback validation remain release work, not an instruction to apply hosted SQL.

## Closure order and non-local gates

First repair confirmed source defects and finish current P8 native tests. Then use one stable production build for all rows above, recording exact route/state, viewport, theme, focus result, issue/fix and post-fix evidence. Include no-env, signed-out, empty, invalid/expired links, loading/cancel/retry, storage failures and account transitions. Add 1366 × 768, landscape tablet, long text and keyboard-overlay checks beyond the common 1280 × 720 / 390 × 844 fixtures.

P9 additionally requires the approved separate acceptance evidence: iPhone Safari browser/installed PWA, Android Chrome, Windows Chrome/Edge, macOS Safari if supported; real camera/mic and native share/download; physical actual-size PDF print; two/four devices on separate networks and forced TURN; measured deployment 1x/2x/5x capacity; five solo creators, five distant pairs, two friendship groups and two hosts with ten guest sessions each; activation checklist and rollback rehearsal; remote CI for the exact authorised commit. Danny deferred phones, which allows local development but does not waive those release claims. No commit, hosted write, deployment or activation is authorised by this queue.

## Relay recovery native follow-up

Relay recovery native receipt, 24/09/2026: `/v2-lab/relay-recovery` passed at 390 × 844 through the real `RoleCapture` and relay recovery helper with synthetic photos, an isolated account-scoped database and camera access disabled by the development-only fixture. Importing four synthetic PNGs under local-save refusal showed the quota error and four original download links, with no cloud request. The links wrapped without horizontal overflow. After disabling refusal, Retry local saving confirmed the save; reopening verified four byte-exact originals and denied the second synthetic account. Stop and clear confirmed removal of the isolated database. This covers the real component/helper and local recovery path, not authenticated legacy relay upload end-to-end, physical camera denial or confirmed OS download delivery.

## Local worker waiting/activation rehearsal

The loopback-only harness is `scripts/sw-update-harness.mjs`. First start the final production build separately on port 3006, then run `node --import tsx scripts/sw-update-harness.mjs --upstream 3006 --port 3007`. It does not start/build production. Open `http://127.0.0.1:3007/__sw-rehearsal`. Use an unused browser origin/port for a fresh run; the harness refuses to replace a worker cache from another run and never clears browser storage. Its proxy forwards only public editor routes/static assets, strips cookies, refuses API/auth/mutation requests, and prevents external connections with CSP.

1. Register A and observe active=activated. Download the provided synthetic `.pbproject`; open Projects in another tab, import it through the ordinary library, and open the editor. Change the caption and wait for Saved on this device. Confirm four originals and Square/JPEG/80% export settings. The synthetic images are plain coloured fixtures, not camera evidence.
2. Keep the editor open. Select Serve release B and Request worker update on the diagnostic page. Record active=activated, waiting=installed and both release-specific static caches. Confirm the original editor remains usable and its latest save completes. The script replaces only the `VERSION` declaration in the actual production `public/sw.js`; it does not add skipWaiting, unregister, update shortcuts or app state mutations.
3. Close every controlled tab on port 3007, including the diagnostic page. The production worker has root scope, so leaving diagnostics open would intentionally prevent natural activation. Reopen diagnostics, then Projects. A missing waiting worker plus the newly created B page cache after public navigation, with A's page cache removed, establishes that B activated and handled the navigation. Preserve the old static caches as the production rollback policy requires.
4. Resume the same project and confirm the changed caption, original inventory and persisted export settings. Record exact run-specific cache names and worker states. Stop the proxy when finished; browser storage remains isolated on this synthetic origin.

Focused harness checks: three tests passed for VERSION-only substitution/no forced activation, real portable-bundle import with four bounded PNG originals/settings, and actual loopback HTTP control/origin/public-route restrictions. Run with `node --import tsx --test scripts/sw-update-harness.test.mjs`.

Native receipt, 24/09/2026: run `a778f058f5` completed on loopback port 3007 against the production app on 3006. Release A became activated/controller. The ordinary project library imported the four-photo synthetic bundle; Export Studio showed Square, JPEG and 80% quality. With the editor open, serving/requesting B produced active=activated and waiting=installed. Editing the caption to `Saved while B waits` then displayed Saved on this device. Both origin tabs were closed without skipWaiting, unregister or storage clearing.

Reopening showed active/controller=activated, waiting=null, `pb-pages-rehearsal-a778f058f5-B` present and A's page cache absent. Both A/B static caches remained, matching the rollback asset policy. The ordinary library resumed the same project: all four coloured originals rendered, the changed caption appeared in the strip and caption field, and Export Studio retained Square/JPEG/80%. This passes local natural worker waiting/activation and saved-draft continuity. It uses one unchanged app build and does not prove compatibility between two full app releases, an active room, OS-installed PWA, real account data or deployed cache infrastructure.

Dropdown fallback rehearsal (24/09/2026): the existing development-only `/v2-lab` now has “Open fallback dropdown rehearsal”. It opens an actual native dialog and forces only the real Dropdown component's non-Popover branch through a development-only argument; production ignores the argument. No browser prototype is modified. Live observations report the portal parent, viewport fit, active option and focus. Exercise ArrowDown twice to skip the disabled choice, Enter to select, End/Escape to cancel, Home/Enter, Z typeahead, Tab, bottom placement, scrolling and the separate dialog action. Close restores the launcher's focus. Seven focused keyboard/placement/production-gate tests passed. Native parent receipt on 24/09/2026 passed at 1280 × 720 and 390 × 844: ArrowDown twice skipped the disabled choice to Bravo, Enter selected it; End then Escape preserved Bravo and left the dialog open; Home/Enter selected Alpha; Z/Enter selected Zulu. Tab cycled through browser chrome and back to the first dialog button. With the trigger at the bottom, live measurements reported inside-dialog=true and fitsViewport=true; the narrow screenshot showed scrollable options within the 844 px viewport. Home/Enter followed by Dialog action incremented its counter to 1, proving the native dialog remained interactive after selection. This verifies the production fallback code path through a synthetic development-only capability override in the tested Chromium browser. It does not establish old Safari, real lack of Popover support, screen-reader or other assistive-technology acceptance.

Incremental relay follow-up, 24/09/2026: capture now awaits an account-scoped checkpoint after each encoded original before advancing. New relay drafts retain a stable URL draft ID; partner capture uses its relay/role checkpoint. Existing saved prefixes must match exact original bytes before append. An unencoded canvas stays available for preparation retry or explicit discard, and a timed-out encoder retains its allocation slot until its actual native callback settles.

The updated isolated native fixture refused only the second checkpoint. It showed one of four originals saved plus download links for both available originals. Remounting from storage reopened the first byte-exact original, denied the other synthetic account and resumed at one of four. Importing the three remaining photos then completed; reopen confirmed four byte-exact originals and account isolation. Stop and clear removed the isolated database. This is real component/repository evidence with synthetic imports, not real camera timing, the authenticated relay page across a browser reload, provider upload or OS download verification.

## Retained local fallback rehearsal, 24/09/2026

A production checkpoint, preceding the later template-row extraction and development-only recovery rehearsal, was copied into a new, checksummed local artifact at `C:/Users/kduy1/AppData/Local/Temp/pb-v2-fallback-20260924-100357`. It contains 31,086 files and 724,999,895 bytes, including the Node runtime, installed dependencies, production output and public assets. Build ID: `i0japautkV19p30eyVGdk`. Manifest integrity: `5d0e7e2bf2596b3b1b49913a5f49f9d041479bb26acb093b3fd7871310aab195`. This Temp copy is retained for local review, not a durable deployed release artifact or the latest working-tree build.

`scripts/local-fallback-artifact.mjs` excludes environment files, Git, caches and private-key filenames, refuses unsafe dependency aliases and checks for private environment material without printing it. Starting the fallback verifies the retained artifact, copies it to an owned temporary runtime and verifies that copy before execution. Only Turbopack dependency aliases resolving inside this repository's installed dependencies are materialised. Runtime environment flags disable new hosted feature activation.

The browser used only the network-restricting loopback proxy at port 3017, with current production on 3006 and frozen runtime on 3016. The proxy blocks external connections, credentials and private/API routes. Its receipt actions separately call the actual TypeScript EventHandler and EventStore against disposable PostgreSQL and synthetic media storage. They do not exercise the frozen Next API, TLS, real authentication or a provider. Eleven focused artifact/proxy tests and the separate actual PostgreSQL integration passed, plus scoped ESLint. A native attempt exposed a five-second request-body timer extending into SQL response work; clearing that timer after the bounded body read passed a real 5.5-second HTTP regression. The corrected rehearsal started with a fresh disposable database before any successful receipt observation.

The ordinary Projects UI imported four synthetic 640 x 480 PNG originals. The editor saved the caption `Frozen build recovery`, and Export Studio showed Square (1080 x 1080), JPEG and 80% quality. Before switching, the accepted private receipt was ready and a wrong token was refused. Pausing admission and restarting the handler increased its generation to 1; the same receipt stayed ready and a fresh reservation returned the expected refusal.

Reloading the editor through the frozen runtime restored all four coloured photos, the caption and Saved on this device. Export Studio retained Square/JPEG/80%, prepared an 18 KB JPG and its ordinary Download JPG action reported that the file was sent to browser downloads. Screenshots confirmed the four-photo result and retained caption. The editable-backup action was exercised, but its browser download event was not observable; neither action establishes OS file delivery. A separate post-switch receipt check again confirmed the same ready receipt, wrong-token denial and new-reservation refusal.

The accepted-work drain returned `acceptedStagedFinalisedDuringPause=true`, `derivativeDeletionConfirmed=true` and `stagingStillCharged=2000000`. The independent ready receipt remained readable throughout. This verifies that the local pause does not prevent an already staged accepted original from finalising, and derivative removal does not incorrectly release outstanding staging charges.

The corrected run used only database `pb_v2_rollback_1790210130456_cc5c1539` and runtime `pb-local-fallback-KV5Zc3`. After the browser checks, both were confirmed absent, with no matching runtime process remaining. The first run's owned database/runtime were also removed after its timeout diagnosis. Browser storage and the retained artifact were preserved.

This proves a bounded local process switch using one unchanged V2 application build. It does not prove compatibility between two different releases, production rollback, real hosted receipt delivery, active-room survival, physical devices or installed-PWA acceptance. Those release gates remain open.
## Genuine localhost Auth, Storage and Events follow-up, 24/09/2026

The [native/service receipt](V2_UI_UX_REVIEW.md#real-localhost-authentication-and-cloud-originals-24092026) supersedes the earlier lack of genuine local Auth/Storage evidence for its explicitly listed flows. Two-account magic-link login, cloud originals, membership consent/revocation and sign-out passed. The [Events receipt](V2_UI_UX_REVIEW.md#real-localhost-events-acceptance-24092026) covers real Next endpoints, Auth, Storage, finalisation, private signed delivery and confirmed derivative cleanup, plus native host settings.

Corrected findings were loopback callback/API origin reconstruction, local export-client configuration, structured missing-object responses and reminder lookup copy. Final build and 61 production HTTP checks passed. Whole TypeScript/ESLint, 65 focused regressions and the expanded 25 rehearsal checks passed. The disposable services and all temporary credentials were removed; ordinary preview remains on port 3005.

This does not establish full authenticated legacy room/relay/vault save journeys, real-time service integration, production TLS/cross-subdomain behaviour, hosted delivery, physical-device/print/share acceptance, assistive technology, cohort/load/deployed rollback or remote exact-commit CI. P9 remains open.
