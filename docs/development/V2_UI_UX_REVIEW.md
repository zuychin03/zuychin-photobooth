# V2 UI/UX completion gate

Danny requires review of every page and component before the V2 goal is marked complete. This register is a working inventory, not a passed review. Refresh it after each feature stage and again against the final build; newly added surfaces must not be omitted.

The local review and corrected findings are recorded in [the P9 checkpoint](P9_REVIEW_QUEUE.md) and the dated follow-ups below. They cover available route entry states, feature-specific synthetic workflows, desktop/narrow/landscape views, custom dropdowns, capture preferences, home discovery/recent projects, wall display, safe fallback pages and confirmation focus. The latest follow-up adds genuine localhost authentication, cloud originals and Events evidence. See [current checks](V2_STATUS.md#resume-checkpoint-24092026) for build and test status. Hosted, device, assistive-technology and the full release matrix remain unverified; do not equate these local checkpoints with the user's overall completion gate.

## Route inventory

| Surface | Required final walkthrough |
| --- | --- |
| Home `/` | Create/Together/Events discovery, project/template resume, narrow layout and optional account entry |
| Booth `/booth` | Camera permission/loading/denial, imports, Classic/Flexible, dropdowns, countdown, save failures, retake/next round, motion controls |
| Editor `/customize` | All nine layouts and templates, crop/filter/scene/layers, loading/fallbacks, undo/redo, autosave, export/share and cloud save failures |
| Projects `/projects` | Empty/list/pagination, resume/rename/duplicate/delete confirmation, imports, future/corrupt recovery and account isolation |
| Cloud projects `/projects/cloud` | Signed-out/unavailable/loading/list/invitation, create, original upload and verification, same-file recovery, cancellation, preview/download, deletion, account isolation and complete reciprocal/partial challenge lifecycle |
| Challenge `/challenges/[id]` | Invalid/unavailable/signed-out entry, return after login, invited acceptance/decline, fresh project access and full challenge lifecycle |
| Templates `/templates` | Empty/list/save/rename/duplicate/delete, import/export privacy, roles/new project and readonly recovery |
| Designer `/templates/design` | All slot/layer controls, keyboard movement, bounded inputs, undo/redo, save conflicts, dirty leave, applying and resource failures |
| Room `/room/[code]` | Create/join/admission, consent/readiness, roster/host controls, synchronised capture, transfer recovery, shared preview, finishing, disconnect/exit |
| Relay `/relay/new`, `/relay/[id]` | Author/guest full lifecycle, invalid/expired/revoked link, private/revealed/partial result, retry and exit |
| Vault `/timeline` | Signed out/empty/loading/error, retained memories, chapters/recap, reminders/timezones, audio and deletion/export |
| Rituals `/memories/rituals` | Unavailable/signed-out/unpaired/loading/error, create/review, legacy upgrade, exact instant, timezone/calendar policy, pause/resume, personal channels, delivery trouble, deletion, access loss and retry |
| Login `/login`, auth callback | Redirect/recovery errors, sign-in/out boundaries and account-draft copy choices |
| Events and receipts, added in P7/P8 | Host/guest/moderator lifecycle, independent publication choices, wall, kiosk turnover, pending queue and expired/revoked access |
| PWA/offline/404 | Updated V2 PWA/app icons (requested 23/09/2026), maskable/Apple crops, install prompt, offline visited pages/assets, unavailable actions, safe update behaviour and meaningful not-found recovery |
| Development lab `/v2-lab` | Probe controls/results and production 404; no device or cloud claims inferred from fixtures |

P7 adds `/events`, `/events/[id]`, `/events/[id]/join` and `/receipt/[submissionId]`, plus development guest/host/export rehearsals. Their local evidence is recorded in [host UI](P7_EVENT_HOST_UI.md), [guest UI](P7_EVENT_GUEST_UI.md), [export rehearsal](P7_EVENT_EXPORT_REHEARSAL.md), [private review](P7_PRIVATE_EVENT_REVIEW.md) and [expiry reminders](P7_EVENT_REMINDERS.md). Reviewed components include `EventHostEntry`, `EventHostWorkspace`, `EventHostDetail`, `EventHostForm`, `EventHostLook`, `EventHostConfirm`, `EventInvitationQr`, `EventGuestEntry`, `EventGuestWorkspace`, `EventReceiptEntry`, `EventExportPanel`, `EventHostTestPreview`, `EventHostContributionPreview` and `EventReminderPanel`.

The rendered P7 pass corrected stale guest controls after session replacement, camera retake preview precedence, duplicate unlabelled file controls, unclear retry-dismissal wording, small ZIP size rounding, singular failed-item copy and stale success notices. Twenty-one native guest journal checks and seven export probes passed. Desktop/narrow light/dark host and export layouts and narrow guest/receipt layouts were inspected. Production guest/receipt fragments were scrubbed and unavailable routes remained usable; private headers and lab isolation were checked. This checkpoint does not complete the final all-page review, forthcoming P8 surfaces, real account/provider operations or deferred devices.

## Shared component inventory

Review `Logo`, `StripMockup`, `CameraPreview`, `RoleCapture`, `Countdown`/flash, `FilterBar`, `CaptureSettings`, `Dropdown`, `PhotoEditPanel`, `ProjectLibraryRow`, `TemplateSourcePanel`, `VisualPackPicker`, `CloudProjectHub`, `CloudLibrary`, `CloudProjectDetail`, `InstallPrompt`, `PushToggle`, account-cookie migration and service-worker status, plus every new export, room, memory and event component. Nonvisual components need verification of their visible effects and failure states.

Each applicable surface needs desktop and narrow rendered inspection, light/dark themes, meaningful focus order/visible focus, keyboard alternatives, readable labels and errors, touch targets, scroll/overflow/dialog behaviour, reduced motion/no-flash choices, and English copy checks. Cover loading, empty, disabled, success, failure, retry and recovery states instead of checking only the happy path. Shared tokens and components should carry fixes to all consumers.

`ChallengePartials` and `ChallengeEntry` join this inventory. The partial-result native review is recorded in [P6 partial UI](P6_PARTIAL_UI.md): explicit proposal/consent/reveal, excluded-proposer secrecy, request failure/retry, native PNG with blank positions, full-challenge cancellation with a still-valid partial, consent withdrawal, keyboard focus and desktop/narrow light/dark checks. Source review corrected invalid frozen retries, stale submission confirmation and interrupted loading; rendered checks covered the confirmation lock and failed-refresh recovery. This does not pass the final all-surface gate.

## Evidence and findings

P2/P3 evidence is recorded in [P2](P2_EVIDENCE.md) and [P3](P3_EVIDENCE.md). Those checks found and fixed custom-dropdown viewport clipping, stale reopening after disable, focus loss after settings autosave, missing designer labels and nine poor portrait scene crops. They do not pass the final all-surface gate while later features are still being implemented.

P4 checks are recorded in [P4](P4_EVIDENCE.md). The rendered export review at 1280 × 720 and 390 × 844 found and fixed lost trigger focus on modal close, editor history shortcuts leaking through the export dialog, an incorrect resource-limit warning caused by pixel rounding, and shortened final video holds. Independent review also fixed sparse animation source mapping, cancelled allocation order, stale motion settings and account/stream identity handling. The dropdown remains visible inside the dialog at narrow sizes, and motion previews require explicit playback. PWA icon raster crops and safe-circle evidence are in [the icon record](PWA_ICON_V2.md); installed-device appearance is pending.

For each final finding record the route/state, viewport, observed problem, severity, fix and post-fix rendered check. Recheck affected shared consumers. Track any externally blocked case separately with its exact prerequisite. Desktop viewport resizing does not satisfy real Android/iPhone, native sharing, physical print or cross-network tests. Do not mark the goal complete with an unreviewed page/component or an unresolved material UI/UX defect.

P5 local review is recorded in [P5 evidence](P5_EVIDENCE.md). Actual two/four-person room interfaces were exercised at 1280 × 720 and 390 × 844, with light/dark themes and keyboard dropdown focus. Fixed fourth-person layout omission, role-gap handling, async focus loss, small touch targets, missing pre-capture editable-copy exit, and misleading removed-guest controls. The save-failure interface retained the photo and blocked an unsafe copy until retry succeeded. Production-unavailable room entry and Projects room-storage empty state were inspected. Final English copy, real-device and complete-build review remains outstanding.

P6 story review is recorded in [P6 evidence](P6_EVIDENCE.md). Four peers completed a guided 16-original round, with the same relaxed prompt and rotating director visible to a guest. The saved composition and narrow guide were inspected. Solo story and timer survived reload; changing a prompt preserved a custom timer. Per-pose countdown boundary tests pass; a fresh native rehearsal displayed the fourth pose's countdown and director before completing 16/16 originals on host and guest. The guest guide was also inspected in light mode. `StoryGuide` and forthcoming memory components join the final shared-component inventory.

The P6 then-and-now review covered normal local original selection, synthetic strip import, pointer/keyboard crop controls, unknown dates, reference persistence, next round, comparison PNG and alternating GIF. Narrow and desktop screenshots found and corrected tiny output labels. The native next-round failure was repaired and retested with the reference intact and no new shots. Motion preparation now locks source edits/capture while leaving Cancel available, verified through the actual format-check cancellation flow. `ThenNowStudio`, `ThenNowGhost` and the changed shared `MotionExportPanel` remain on the final component register. The booth uses its existing forced-dark capture surface; actual camera overlay alignment and real-device delivery remain pending.

A further keyboard pass found that focusing an unpositioned, visually hidden file input scrolled the entire desktop booth out of view. Positioned labels now contain the reference, booth-photo, template-photo and designer-decoration inputs. The reference control and template-photo editor control were retested with visible focus and stable page position. This is a rendered accessibility fix, separate from the final full-surface gate.

The new cloud entry page was inspected signed out at 1280 × 720 and 390 × 844 in dark mode. It explains that signing in does not upload device projects and provides working sign-in/device-library links. Full cloud workflow review is being performed in a development-only transport fixture; real authentication and hosted Storage remain pending. Independent review caught and corrected a same-file resume filter mismatch and deletion wording that overstated revocation of already issued links. Account changes close the client and journal, hide the workspace, abort local work and release previews.

The cloud fixture review now covers private/shared project creation, invitation acceptance, explicit original preparation/upload, pending verification, stop-waiting, workspace unmount/remount recovery, successful verification, digest-checked preview, failure/retry, account switching, original deletion and deletion of an empty disposable project. Both light and dark narrow layouts were inspected. The custom project-audience dropdown is reused for private/friends selection. Membership review used two synthetic accounts: the invited member accepted and uploaded an original; the owner could preview it but could not individually delete it; removing access cleared the existing preview, retained the documented ordinary shared original, and prevented silent reinvitation. Confirmation opening/cancelling and post-action headings were inspected for keyboard focus. A timing-sensitive focus issue was corrected by focusing after the confirmation renders. Cloud file selection now targets the explicit upload action after local preparation, and verified completion returns focus to the project heading. `CloudMembers` and `CloudInvitationId` join the final component inventory.

All 11 native journal probes passed, including a separate module worker rejecting stale account reads/writes after main-context removal. These checks use real browser IndexedDB but synthetic transport/accounts; they do not establish hosted authorisation or production Storage behaviour. Final English copy and every-page/component review remain outstanding.

Post-review recovery fixes were retested natively: a pending upload remained manageable in `CloudUploadRecovery` after its project was deleted, explicit dismissal removed only its local tracking, and focus returned to the recovery heading. Membership changes disabled all competing detail actions during their request. Local file preparation focused Upload original; dismissing a prepared record focused its confirmation and returned to the project heading. Independent sign-out cleanup failures are covered by five meaningful store-isolation tests. `CloudUploadRecovery` joins the final component inventory; its final narrow/English copy review remains pending.

The primary reciprocal challenge flow was reviewed in the actual cloud components using two synthetic accounts. Creation, acceptance/opening, sparse source assignment, protected upload, account-based recovery after completed tracking was cleared, submission/retry, reveal and withdrawal passed locally. Native PNG preview was inspected at 1280 × 720 and 390 × 844 with light/dark themes. Confirmation/success focus and custom-picker duplicate-source prevention were observed. Actual OS download delivery remains unverified. `ChallengeWorkspace`, `ChallengeCreate` and `ChallengeDetail` join the final component inventory; partial consent/export screens, challenge camera capture, English copy and physical devices remain pending. See [challenge UI evidence](P6_CHALLENGE_UI.md).

`RitualHub`, `RitualWorkspace`, its editor and channel-choice form join the final inventory. The [ritual UI pass](P6_RITUAL_UI.md) covered native timezone preview, lost acknowledgement/retry, legacy upgrade, pause/resume, recipient isolation, explicit deletion, access loss, custom dropdowns and focus at 1280 × 720 / 390 × 844 in light/dark themes. Native date/time input event handling, title-only one-off preservation, late account-listener teardown and stale pairing display were corrected before that checkpoint. This is local synthetic-transport UI evidence; live auth, native notifications, English copy and physical phones remain unverified.

`CloudDesignPanel`, `CloudDesignSavePanel`, `VoiceCaption` and `VoiceMemoryEntry` join the final component register. The [cloud design rehearsal](P6_CLOUD_SAVE_JOURNAL.md) covers local preparation, lost-response remount/reconciliation, editable-copy adoption, versions, stale/revoked refusal, account isolation and confirmation focus. The [voice rehearsal](P6_VOICE_UI.md) covers native drafts, recording, pointer playback, save/delete retry, stale conflict, safe confirmation and access loss. These local passes do not complete the final route/component gate or authenticated/provider/device acceptance.

The [group partial rehearsal](P6_GROUP_PARTIAL_REHEARSAL.md) adds four-person consent/recovery, an excluded proposer, a native 4060 x 3044 partial with blank columns, and full-challenge cancellation without discarding the partial. The [guided-story review](P6_ASYNC_STORY_UI.md) adds three/four-person layout choices and preserved-draft older-server fallback. A faulty development expiry shortcut was corrected to preserve the immutable deadline; same-client refresh was retested. These are local synthetic-transport checks, not final hosted/device acceptance.

P8 audience and host publication checkpoints are recorded in [audience browser evidence](P8_AUDIENCE_BROWSER.md) and [host publication review](P8_HOST_PUBLICATION_UI.md). Six native audience probes passed. Gallery reporting/download, independent wall access/rotation, withdrawal, pause and expiry were rendered; report/pause/download focus corrections were confirmed. Host review covered four consent combinations, lost approval response, stale approval refusal, re-consent, hide/remove, reports, scoped QR links and account loss. Desktop/narrow and light/dark surfaces were inspected. `EventAudienceEntry`, `EventAudienceWorkspace`, `EventModerationPanel`, `EventModerationPhoto`, `EventAudienceLinks` and `EventHostGuestbookPanel` join the final register. Kiosk, remote postcards, guest consent and the final complete route/component pass remain outstanding.

## Template focus follow-up, 24/09/2026

The normal template shelf duplicated a synthetic recipe and renamed it. Native review exposed focus returning to the page body after the row refresh. The corrected shelf focuses the stable My templates heading after successful mutations, focuses the name input when Rename opens, chooses Keep template when the delete confirmation opens, and restores Delete when cancelled. Each corrected path was exercised in the native browser. No template was deleted during this pass.

The shelf was visually inspected at 390 × 844 with wrapping controls. The normal designer added a text layer, warned before leaving, retained edits on Keep designing, then undid the layer and saved the fixture. Its confirmation now focuses Keep designing and cancellation returns focus to Templates. Fresh-load verification confirmed both focus targets and no horizontal page overflow (385-pixel scroll width in a 390-pixel viewport). These are targeted local template checks; future-record recovery, save-conflict failures, physical keyboard overlays and assistive-technology acceptance remain separate.

## Legacy room follow-up, 24/09/2026

The development-only `/v2-lab/legacy-room` mounts the actual legacy interface and peer engine with two synthetic canvas streams, explicit local BroadcastChannel signalling, no ICE servers and separate disposable project databases. Both peers connected. A refused host save retained the original and exposed the normal retry action. The guest also retained two originals while the two fixture peers competed for one page's encoder; separate devices do not share that encoder. Retrying both peers completed their normal `/customize` handoffs. Each database reopened eight originals, four from each role, with all eight SHA-256 hashes matching the saved source bytes. Disconnecting the guest displayed Everyone left the room on the host. Stop and clear confirmed both streams, engines and fixture databases were removed.

Rendered review found two layout defects. At 390 × 844, viewport-based pane sizing let previews overlap the controls. Panes now use the stage's actual content height: after correction both preview bottoms were at 206 px, above the controls at 273 px, with no horizontal page overflow. At 1280 × 720, centring an overflowing desktop sidebar put the first layout control at -165 px even with scrollTop zero. Safe centring now places it at 24 px; scrolling reaches the shutter at 564–644 px. The custom motion dropdown opened within the narrow viewport, and Escape retained its prior selection. The capture surface intentionally remains dark even when the fixture shell uses the light theme.

This covers the local legacy interface, original storage, retry and peer disconnection. It does not prove physical camera behaviour, real-person segmentation, separate-network signalling/TURN, authenticated route reload or OS delivery. The fixture records navigation requests rather than replacing the actual editor handoff checks elsewhere in this register.

## Template recovery and framework error follow-up, 24/09/2026

The existing development-only `/v2-lab` now mounts the production `TemplateRow` extracted unchanged from the ordinary shelf. Synthetic future-version and damaged records show the read-only message. The actual rendered row contains only Recovery files and Delete, plus Raw recipe and Decoration 1 after recovery opens. Apply, new project, design, rename, duplicate and ordinary export controls are absent even with `canApply` true. The private-text/account-details warning is visible. Raw JSON and PNG actions were exercised; OS file delivery was not established. The fixture reads or writes no ordinary shelf data and does not replace the earlier storage-parser tests.

At 1280 × 720 and 390 × 844, warning text and controls wrap without horizontal page overflow. Opening Delete focuses Keep template; keyboard cancellation restores Delete. Both future and damaged fixtures use the same production recovery row. This closes the specific rendered future/damaged-row gap in the earlier template receipt, not the remaining physical keyboard or assistive-technology checks.

Trigger test error throws during render into the real `app/error.tsx` framework boundary. At both desktop and narrow sizes its heading receives focus and its copy and actions fit. In the narrow keyboard pass, Tab moves to Try again and Enter returns to the healthy lab with its fixture state reset. Desktop pointer retry also succeeds. The development issues badge is expected for this intentional error. No custom error boundary or browser-state patch was used, and the temporary viewport override was reset after inspection.

## Designer competing-save recovery, 24/09/2026

Two ordinary designer tabs on the separate `localhost:3005` origin opened one synthetic blank template. The first saved a changed caption. The second correctly refused to overwrite that newer revision, retained its unsaved caption, and offered Save as a copy. The conflict warning wrapped at 390 × 844. This used the normal shelf and real IndexedDB transactions, without a storage failure override.

The check exposed a navigation defect: saving a copy left the URL pointing to the original template, so reload reopened the original even though the copy was safely stored. Initial saves likewise did not acquire a saved-template URL. The designer now replaces the URL with the successfully saved ID and actual scope after the existing lifetime/account check. A synchronous action lock and busy state prevent edits during the save/navigation handoff; decoration blobs are captured before awaiting storage.

Post-fix native checks confirmed that an initial save gained its own URL and restored the saved name/caption on reload. A repeated two-tab conflict was refused; Save as a copy then changed to a different template ID. Reloading both tabs preserved `Original saved from first tab` in the original and `Recovered competing edit` in the named copy. The copy remained editable. Whole TypeScript and scoped ESLint passed. The task-created recipes contain no photographs or account data; browser tabs were closed and the viewport override was reset.
## Real localhost authentication and cloud originals, 24/09/2026

An isolated app copy on port 3010 used genuine local Supabase Auth, PostgreSQL and Storage with migrations 001–028. Only synthetic accounts and an existing app icon were used; the normal port 3005 preview and hosted configuration were unchanged.

- Native PKCE magic-link sign-in passed on both `127.0.0.1` and `localhost`, including the requested cloud-library return path. A real defect was corrected: Next development URL reconstruction changed the callback hostname and rejected same-origin API requests. The shared origin helper now preserves validated loopback Host values at the same port in development; production keeps its canonical HTTPS origin and ignores forwarded-host overrides.
- The owner created a shared cloud project using the themed dropdown, selected an 8.6 KiB 192 × 192 PNG, explicitly uploaded it, and recovered its pending confirmation with Check status. The actual project worker verified the stored bytes. Signed preview decoded at 192 × 192 in the browser.
- A second account initially had no projects. It saw the invitation and project name before accepting, then gained access to the original and decoded its signed preview. After owner revocation, another preview request was refused, its displayed image was cleared, and refreshing the library removed the project.
- The authenticated legacy vault loaded its empty and pairing state. Sign out and keep drafts reached the signed-out confirmation. Fresh owner sign-in returned to the original host and reopened the existing cloud library.
- A separate real Storage API check verified owner upload and byte-exact read, refusal of anonymous and unrelated-user reads/signing, and confirmed deletion of its separate synthetic object and accounts.

The first upload chooser became stale during development refresh; reopening the saved project and using a fresh bounded chooser succeeded. A consumed email link showed the expired-link error; a fresh link completed successfully. These checks do not establish physical camera/audio, OS download delivery, hosted email, production TLS/cookies, or cross-network rooms.

## Real localhost Events acceptance, 24/09/2026

The actual Next API, genuine Auth and Storage and migrated database passed a separate synthetic event lifecycle: creation, opening, invitation redemption, reservation, signed PNG upload, finalisation, ready receipt, signed JPEG download and 64 × 64 decode. The first worker attempt exposed a real provider mismatch: Storage 1.72.1 returns missing objects as HTTP 400 with a structured `NoSuchKey`/semantic 404 response. The event, project and voice object adapters now recognise only the exact bounded envelope after checking response origin/path. Permission failures, generic 400s, malformed data and excessive bodies do not confirm absence. HEAD 400 cleanup checks use one authenticated GET probe, cancelling a present object's body immediately.

After that correction, the retained event job completed without another upload. Host removal changed the private receipt to deleted; new media access was refused. The real worker deleted both derivatives and confirmed their absence. The dashboard reported zero derivative bytes and retained the 2,000,000-byte staging/token safety charge. Existing issued signed URLs were not claimed to become immediately invalid.

The native host workspace created a separate draft with a 16 MB allowance. The custom scene dropdown saved Celebration Garden and the caption `Local V2 celebration`; refresh retained both. Local HTTP Storage was allowed by the export client only for an already validated local HTTP app origin, preserving hosted HTTPS requirements. Export discovery recovered from initial route compilation through Refresh export progress. The disabled reminder service now shows read-only unavailability guidance, while uncertain saves retain their exact-request retry wording. The corrected reminder message was verified in the browser.

Whole TypeScript, full ESLint and 65 focused regressions passed after the service/client changes; the final reminder-copy edit passed scoped lint. This is local service and native UI evidence, not deployed scheduler cadence, provider email delivery, physical devices or production acceptance.

## Shared navigation and copy refinement, 24/09/2026

Danny requested a relocated logo, shorter copy and contextual tooltips, then specified a two-colour Gallery-style wordmark and the same navigation across pages. `SiteNav` now appears once in the root layout, with active-route styling and the existing account-dependent links. Its surface is transparent and borderless. The wordmark reads `Zuychin / Photobooth`, with Photobooth in the rose accent. Home reads `Your memories, your photo stories` and `Make photo strips solo or with your group of friends from anywhere.` The final phrase and help icon stay together when wrapping. Home and sign-in backgrounds extend behind the navigation.

Repeated introductions and implementation explanations were shortened across capture, editor, projects, templates, Together, Memories, challenges and event components. Sixteen contextual help placements use one `HelpTooltip` component. Consent, expiry, privacy, destructive actions, availability failures and recovery guidance remain visible. Tooltips support pointer entry, keyboard focus and click/tap; they use the top layer with a dialog-aware fallback and inherit the current theme.

Native checks visited Home, Templates, Frame designer, Events, Together, Booth, Customize, Projects, Cloud projects and Sign in. Review included 390 × 844 and desktop layouts. At desktop 1280 × 800 the booth's measured main height was 732 px below the 68 px navbar. The editor preview, scrollable tools and export footer fit beneath the same navigation. The camera-denied state stayed visible; no physical camera permission was granted. Mobile Home had no horizontal overflow. Its final phrase and icon occupied the same 32 px-high inline group, and the header computed to transparent with a zero-width bottom border.

An unsaved template-name edit followed by the Events navbar link stayed in the designer and focused Keep designing. Cancellation restored focus to Events. Repeating the navigation and choosing Leave designer reached Events without saving the temporary name. Normal navigation from the saved editor reached Projects. The home help opened by click and keyboard focus and closed with Escape. In the actual export modal, the photo-detail tooltip entered the top layer; Escape removed only the tooltip and left the modal open. The modal's own close action then worked. Global navigation guards were also source-reviewed across pending captures, cloud/event operations and memory drafts; those additional authenticated branches were not exercised in this pass. Kiosk privacy interception is retained and the shared navbar becomes non-navigable while the existing lock is active; a new real locked-kiosk journey was not run.

Whole nonincremental TypeScript passed. Full ESLint returned one dependency warning in the new shared hook; it was corrected and the affected shared files passed scoped ESLint. The final production build after the sign-in background correction passed compilation, TypeScript and all 31 static pages. The ordinary development preview remained available on port 3005. The viewport override was reset. These checks do not close the remaining authenticated, physical-device or release acceptance gates. No new feature-guide document, commit, push or deployment was made.

## Single-row navigation, 25/09/2026

The shared navigation pill now keeps every option on one line. Overflow enables horizontal scrolling and two 44 px chevron buttons that jump to the start or end, disabling at their respective endpoints. Wider 16 px link padding, pink hover, the transparent header and the mobile account icon are retained. The wordmark uses the app's Fraunces font.

Native Home checks at 320/375 px confirmed one row and no page-level horizontal overflow. Both chevrons reached the correct endpoint, and a direct horizontal scroll returned to the first options. Keyboard review exposed Templates being only partially visible; the corrected focus handler reveals the complete focused option within the scrollport. Retesting reached a fully visible Templates label and focus outline. At 1280 px every signed-out option fit and both arrows disappeared. Returning to 375 px restored the arrows correctly. The Together page retained the same pill and correct active styling. The temporary viewport override was reset and the ordinary home preview was left available on port 3005.

Whole nonincremental TypeScript and scoped ESLint passed after the focus correction. A subsequent genuine sign-in check covered the longer authenticated menu at 320 px: horizontal scrolling reached Memories and Vault, the end button disabled correctly, and page width remained within the viewport. These checks cover desktop browser viewport behaviour, not physical phone gestures. The remaining P9 gates stay open.

## Real paired relay and account isolation, 25/09/2026

The existing six-container disposable Supabase stack was recovered with its database intact. Auth/Storage schemas and migration 028 capability were verified before finish-only preparation. Published API, database and mail ports remained bound to 127.0.0.1. A fresh isolated app copy on port 3010 included the current shared navigation; no hosted configuration or data was used.

Two genuine local accounts signed in on separate loopback hostnames. Alex created a pairing code and Bao joined through the normal Vault form. Each account imported four existing, non-personal app icon PNGs through the actual relay capture file chooser. The first upload produced Your half is saved, Waiting for your partner for Alex and Your turn to finish a relay for Bao. The partner completed the relay and reached the ordinary editor. Saving to the Shared Vault produced a PNG that decoded at 1072 × 3044 for both accounts. Only the owner received keep/delete controls. The completed relay disappeared from the pending list, and reopening its direct route successfully loaded both halves back into the editor.

Desktop and 375 px capture review confirmed that Back sits below the shared header and the import/feedback controls remain reachable. The normal editor and save action were inspected at narrow and desktop sizes. No physical camera permission was granted. The pairing input lacked an accessible name; `Pairing code` was added and appeared in the native accessibility tree.

The Unpair dialog initially focused Stay paired. Both Escape and button cancellation preserved the pairing and restored Unpair focus. Ending this disposable pairing focused the Vault heading and retained Bao's saved strip. After refresh, Alex's Vault no longer contained the partner strip; the old relay URL returned Relay not found. Read-only database inspection confirmed zero rows for that relay and one cleanup job with status queued, stage pending and zero attempts. This proves cleanup admission, not physical object deletion or immediate invalidation of already issued signed URLs.

On the same origin, Alex's project library contained the relay originals and completed editor draft. Sign out and keep drafts hid both. Signing in as Bao on that origin showed no local projects, while Bao's own server strip remained available. Signing out and restoring Alex brought both original account drafts back. No local copies were deleted.

Whole nonincremental TypeScript and scoped ESLint passed for SiteNav, Vault and relay. The default production build could not fetch Google Fonts; a Webpack build using the existing owned cached-font mock then passed compilation, TypeScript and all 31 static pages. Both development previews returned HTTP 200 afterward. The viewport override was reset. Real concealed challenges, the full event guest/moderation/export journey, kiosk isolation, physical devices and external release acceptance remain separate.

## Real concealed challenge lifecycle, 25/09/2026

The genuine local Auth/Storage app on port 3010 completed an ordinary two-account challenge without fixture transport or browser-state injection. Alex created the shared project `Local concealed challenge 25 Sep`, invited Bao, selected the Taking turns layout and fixed the When everyone has submitted reveal policy. Bao accepted the project and challenge through their normal pages. Reload preserved the invitation; the owner could open contributions only after acceptance. Assigned photo positions were Alex 1/3 and Bao 2/4.

Four existing neutral app icon PNGs were chosen through native file pickers and explicitly uploaded. One picker returned after about 34 minutes; the first reservation subsequently expired. The worker verified three originals and swept one expired reservation. The app's Check status changed that original to Upload failed and kept submission disabled. A new selection/upload and one worker pass recovered the missing original. Both accounts then had two verified originals. Recovery dropdowns restored ready originals after a development remount and disabled selecting one original for both positions. Bao's original preview decoded at 180 × 180.

| Phase | Actual service evidence | Native browser evidence |
| --- | --- | --- |
| Alex submitted, Bao waiting | Both views open. Alex sees two own sources; Bao sees zero submitted sources. All four own reads sign/download with HTTP 200 and exact byte count/SHA-256. All four cross-account reads return 403 `access_denied`, without a signed URL. | Alex shows Submitted and only own original buttons. Bao cannot preview Alex's contribution. Submission requires a separate confirmation. |
| Both submitted | Both views revealed, accessLost false, four visible sources each. All four cross-account reads sign/download with HTTP 200 and exact bytes/hash. | Both shared previews decode at 1072 × 3044. Download shared PNG reports preparation. Low-resolution source warning is expected for the small neutral icons. |
| Bao withdrawn | Both views retain status revealed but accessLost true. Bao is withdrawn; each sees only two own sources. Own reads remain byte-exact HTTP 200; all four fresh cross-account reads return 403 without a signed URL. | Cancellation preserves the preview. Confirmed withdrawal clears Bao's shared preview/actions. Refreshing Alex clears the already open preview and both full-result actions; each account can still access its own originals. |

Desktop and 375 px challenge/result layouts were inspected; the narrow page had no horizontal overflow. The custom recovery dropdown and confirmation focus worked. Full-result preview count and action count were both zero after the partner refreshed withdrawal. The temporary viewport override was reset. Existing signed URLs and downloaded copies are not claimed to be recalled; OS file delivery was not inspected.

Two earlier cloud-library forms/details and later challenge selections reset around development Fast Refresh events. Read-only review found matching source copies, stable same-owner auth callbacks and no proven application remount cause. A warm-route or production-runtime reproduction is still needed; no speculative source fix was applied. The normal challenge page also retains repetitive outer/detail headings and secondary explanation text, which belongs in the remaining copy pass. Camera, immediate-reveal/partial-result ordinary-account branches, Events/kiosk and external/device acceptance are not certified by this concealed two-person journey.

## Real event guest and navigation follow-up, 25/09/2026

The retained event `Local event acceptance 25 Sep` now has a genuine guest submission, `6ad36bc9-3a88-4f1b-8f04-7c05419372cd`. The ordinary invitation joined successfully; a neutral 512 px app icon was selected through the native file chooser and prepared as a 1072 x 1336 finished event photo. Private delivery and gallery were selected independently, while wall consent stayed off. Explicit reservation produced a private receipt before upload. The actual event worker verified the uploaded photo; native Refresh status showed Ready for private download. Host moderation decoded its 321 x 400 thumbnail, kept wall approval disabled, and approved only the gallery after a separate confirmation. Cancelling the first confirmation did not publish.

The actual private receipt opened and downloaded a 61,751-byte JPEG into the local Downloads folder. Sharp fully decoded it at 1072 x 1336; SHA-256 was `5ceeeb07b5142de097f6c20ac8436eae348e5ef230eb6a97dcc5984c28fbcc09`. The older similarly named file was preserved. The host then created a separate gallery capability through its confirmation, and the ordinary viewer opened the approved photo with a decoded 321 x 400 thumbnail. This proves one local browser's file delivery and gallery access, not mobile/OS coverage or a full event rehearsal. Subsequent guestbook, gallery-removal and export checks are recorded below.

The gallery's first development request spent 15.4 of 15.8 seconds compiling. Browser HMR reconnects coincided with entry resets; no concurrent application-source edits were found. The consumed URL fragment was lost on a remount before exchange. Reopening the original disclosed link after warming the route, then explicitly reloading, recovered viewing access. The existing guest and receipt tabs also reset during this period. A production-runtime acceptance pass remains necessary to separate development refresh behaviour from an application defect; no speculative fix was applied.

The resumed guest session recovered its ready submission and saved a private message and typed signature. The host's private guestbook page read both saved values. A resumed gallery session decoded the approved thumbnail again, with neither the message nor signature present in the rendered audience page. Host mission settings also loaded after retry; the server log recorded a successful 10.2-second response with 9.6 seconds spent in development compilation, exceeding the client's ten-second deadline.

The host then hid only the gallery destination. Its exact retry confirmed hidden state; the audience's fresh check showed No photos to show yet and zero photo elements. The guest resumed its existing session and refreshed status, receiving Your photo is ready with private download still enabled. This verifies refreshed gallery removal and retained private-delivery status, not recall of existing downloaded copies or a fresh post-removal binary download.

A separate wall access link opened the actual wall viewer after warming and reopening its original link. It showed no photos, consistent with this contribution's withheld wall consent and absent wall approval. Start display hid the wall's ordinary controls and focused Exit display; Escape restored the Live event wall heading and ordinary controls. The shared app navigation remained available. This covers this one no-wall contribution, not all four permission combinations against genuine services.

Snapshot `727c5978-7a78-422a-be77-b950c3919aa1` recovered through Retry exact snapshot, prepared its one-photo ZIP and reopened with prepared progress in a fresh host tab. Native Download ZIP reached Download requested, but no matching file appeared in Downloads and two download-event observations timed out, including one in the fresh tab. No saved confirmation was clicked; prepared page blobs were explicitly released. A separate read-only genuine-service validation assembled and inspected the same ZIP in memory: 63,937 bytes, four entries, one byte-identical 61,751-byte JPEG, matching private guestbook text, a valid manifest, valid entry CRCs and zero photo/guestbook failures. Snapshot revision and progress stayed unchanged. This verifies service-backed ZIP content, not native ZIP delivery; the latter remains unresolved.

Moderation cancellation focus was corrected in the main checkout. The trigger is captured before its controls become inert; a removed or disabled trigger falls back to the moderation heading. Native host-rehearsal checks confirmed Keep working and Escape both restore the exact Approve gallery button while leaving the photo awaiting approval. Eighteen existing event action/fixture/moderation tests, scoped ESLint and whole nonincremental TypeScript passed. The 3010 acceptance copy was deliberately left unchanged during active event requests.

The final combined checkpoint passed whole nonincremental TypeScript, repository ESLint and 1,139 tests with zero failures, skips or cancellations. The obsolete home-hero logo assertion was updated to check shared SiteNav placement; its geometry and asset checks remain intact. Targeted branding tests passed 7/7 with scoped lint. No new production build was run.

The missions endpoint returned 503 because its raw PostgreSQL timestamp used an offset and the shared browser parser requires UTC Z. Server normalisation now covers missions, private review, reminders, postcards and publication boundaries while preserving strict client validation. All 43 affected tests, scoped ESLint and whole nonincremental TypeScript passed; the five stores were synced to the owned 3010 copy. Their remaining native surfaces have not all been rechecked. Export and kiosk contracts already normalise offset timestamps.

The shared navigation now spans the viewport, with 20 px narrow and 32 px wider gutters. Only the desktop pill pins after passing its origin; mobile has five icon-and-text destinations with direct Templates. Desktop opacity is 75%, mobile 85%, with the retained 24 px blur. A real booth screenshot and hit tests reproduced page controls covering the mobile tray at the same stacking level. Both navigation surfaces now use level 45, above page controls at 40 and below custom overlays at 50. All five mobile labels passed hit testing over scrolled booth content. Templates navigation worked; the actual export dialog covered navigation, and its Close restored Export focus. TypeScript and scoped lint passed. Viewport overrides were reset. Phone hardware and the overall all-surface completion gate remain open.

## Genuine cloud versions and initial export loading, 25/09/2026

The ordinary UI saved version 1 of private project `327206e9-ce75-42ce-aff0-1f30c299e198` (Local editable design 25 Sep) from eight neutral originals totalling 679 KiB, using finite local worker passes. Genuine-service checks matched all eight originals' hashes, bytes and dimensions; Bao was denied project access and received HTTP 403 for all eight originals. Reopening version 1 as an editable copy, changing its caption to `Cloud checkpoint two` and saving confirmed version 2. After reload, opening the latest version into an editable copy preserved that caption and the composited icon preview. Selecting the previous checkpoint, Version 1, opened a separate editable copy with its original empty caption. Read-only database checks confirmed revisions 1 and 0, eight identical asset bindings, distinct request IDs and content hashes, two receipts and only eight ready asset rows.

The save coordinator now reconciles stale per-original upload tracking through bounded status checks when resuming an in-progress save, without resending bytes. Scope and frozen-descriptor mismatches still fail closed. Its 26 focused save/upload/fixture tests, scoped ESLint and whole nonincremental TypeScript passed. The patch was synced to the owned 3010 copy after version 2 was saved; it does not automatically sweep historical completed requests. Existing per-original Check status remains the explicit recovery path. The latest mobile navigation, creation-capacity message and reminder/export coordination components were also synced with matching hashes.

On a fresh native Events entry, Open event first waited for the host load and initial reminder read. After the reminder read failed, the existing one-item export snapshot `727c5978-7a78-422a-be77-b950c3919aa1` appeared automatically, without Refresh export progress or the earlier false export cancellation. The coordination change passed 36 reminder/export tests, scoped ESLint and whole nonincremental TypeScript. An actual reminder read returned HTTP 503 with `unavailable`: the owned localhost launcher explicitly sets `PB_EVENT_REMINDERS_ENABLED` false. This is an expected disabled-service boundary, distinct from an enabled service with unconfigured delivery channels. No flag was changed. These results do not establish a stable production runtime, physical-device acceptance or native ZIP delivery, which remains unresolved above.

## Genuine memory labels and chapters, 25/09/2026

Alex's ordinary Memories page listed ten available originals. Opening the newest memory rendered its actual neutral icon after the source-access check. The native form created chapter `Local V2 memories`, then saved occasion `Cloud keepsake check` and assigned that chapter. Both saves were explicitly confirmed. Reloading retained the chapter and occasion; selecting the chapter filter returned the one labelled memory, and its original opened again.

A scoped read-only database transaction confirmed chapter `c56f9660-c368-4f77-9055-e85c53eac92b` at revision 0 and memory `8a35aec9-dd41-490e-9a43-141f4b29c6a1` at revision 1 with the expected values. The actual visibility predicate allowed Alex and denied Bao; Bao had zero matching chapters or labels. Earlier binary integrity checks cover the unchanged source. This closes ordinary-account memory save/reload/source access, not paired ritual persistence, voice recording or external notification delivery.

## Genuine paired ritual persistence, 25/09/2026

The ordinary ritual page first showed Choose your photo partner first for Alex. Alex created a pairing code and Bao joined through the retained localhost session. A scoped database read confirmed pair `677e573b-8e47-4930-a944-8eab2e39e6a2` contains exactly those two disposable accounts. Alex reviewed and saved `Local weekly photo date`, weekly from 26/09/2026 at 19:00 Australia/Sydney; both the review and database agreed on 09:00 UTC. Ritual `120642e1-5baa-407a-a000-0313490c8024` had two recipients, initially both opted out.

Alex paused the ritual, which Bao's ordinary page also showed as paused. Bao had My reminder choices but no schedule editing/pause controls. Alex resumed it; both accounts reloaded the scheduled occurrence. Alex then saved email on/push off while Bao's refreshed view remained email off/push off. A read-only check confirmed revision 3 and exactly those per-member flags. Alex's reload retained the preference, then an explicit save restored email off and confirmed that Bao's choices were unchanged. Both finish with email/push off. No sender, reminder worker or external notification ran. This closes paired schedule/pause/resume/reload and preference isolation through actual local services; synthetic DST/failure tests and external delivery gates retain their separate scope.
