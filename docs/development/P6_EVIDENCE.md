# P6 local implementation evidence

Updated 23/09/2026. P6 is in progress. This record does not pass the whole-stage or final all-page review gate.

## Guided four-cut stories

Twelve versioned authored decks cover date, friends, celebration and seated/gentle poses, with English prompt text. A stored seed assigns stable participant director turns. Each prompt can be replaced with a relaxed alternative while retaining all four capture positions. The host orders room changes; guests receive the same saved plan. Solo story, layout and timer changes share one durable project revision. Replacing a prompt preserves a timer chosen by the user.

The local project schema advances from 2 to 3 with additive story metadata. Versions 1 and 2 retain identity, source media, history and fixed capture timestamps. Unsupported future schemas stay read-only. A four-person story uses the new 16-cell `quad-story` layout; the existing three-shot Quad remains available.

Native browser rehearsal at `/v2-lab/room` used four synthetic cameras, actual RTCPeerConnections and isolated IndexedDB stores. All four participants were admitted. The host selected Pass a smile and replaced photo 2; Dara independently displayed the same relaxed prompt and Alex as director. The timed capture completed with **16/16 originals on both the host and Dara**, four per participant. The saved 4-by-4 composition was rendered and inspected. The guide, focus treatment and wrapping were inspected at 390 × 844. Initial connection diagnostics reported recoverable reconnects before capture; this is not evidence of cross-network or real-phone reliability.

The normal solo booth retained Quiet company, a relaxed photo 2 and a five-second timer across an actual page reload. Camera-unavailable fallback and imports remained accessible. No camera permission was accepted or real camera capture claimed. The per-pose room countdown uses the same shared step clock. A fresh four-peer rehearsal after the security update displayed photo 4, Dara as director and a countdown of eight seconds while 12/16 originals were already saved. It then reached 16/16 on both the host and Dara. The guest guide was also inspected in the light theme, including photo 2 and Bao as director. The fixture was stopped and released after the checks.

Focused regression: **88/88 tests passed** across stories, additive project migration, layouts/composition, recipe authority, capture sequence and room controller. Whole TypeScript and focused ESLint passed. One initial test used a nonexistent layout ID; the fixture was corrected to the existing `strip3` before the passing run. These checks are earlier than forthcoming then-and-now and challenge integration; rerun the whole suite/build for the P6 checkpoint.

## Then-and-now local workflow

The solo booth now imports an old image or copies an original from the current local account/device scope into its own project inventory. Reference bytes, fixed optional date, crop and alignment persist separately from camera originals. They do not populate capture slots or change the capture timestamp. Removing the guide retains its bytes for undo and portable backup; reusable frames exclude that private reference.

Native browser checks used synthetic local images only. A saved original was copied through the project/source dropdowns; zoom 1.05 and the reference date survived reload. Four normal imports produced a dated comparison and an explicit-play alternating GIF. The first narrow review found undersized date text; width-scaled typography and matching camera aspect corrected it. Fresh screenshots at 390 × 844 and 1280 × 720 show readable dates, visible keyboard focus and contained dropdowns. The booth intentionally uses the existing dark capture surface in both system themes.

The next-round walkthrough found an initial revision mismatch when carrying the reference. A dedicated initial-project factory now stores the reference atomically at revision zero. Retesting through the normal button produced zero new shots while preserving the guide, zoom, story, five-second timer and saved frame. The previous round remains independently saved.

A 400 × 1000 synthetic flattened strip was imported through the native file picker. Pointer and keyboard crop controls selected its top panel with height 0.23. Clearing the optional native date field and saving produced **Unknown date**. The comparison identifies **Selected strip crop**; a whole-image selection has separate honest wording. Native GIF preparation produced a **13 KB, 640 × 536, 3.0-second** alternating file with no autoplay. Earlier PNG export reached the browser download action; OS file delivery is not established.

Reopening retained the crop and unknown date. The import-type control now starts from the stored reference kind and explicitly describes the next import. A keyboard pass caught an unpositioned screen-reader-only file input that scrolled the desktop booth completely out of view. Positioning the wrapping label bounds the hidden input and restores a visible focus outline. A fresh Tab traversal kept document scroll at zero, with the input inside its 44-pixel label. The matching template-photo import pattern was corrected and its editor focus was rendered successfully as well.

Independent review found that edits during motion preparation could silently invalidate the result. The shared motion component now reports busy state; reference controls and next round lock while Cancel remains available. Native format-check cancellation confirmed all three states and returned to preparation with the still photos intact. The subsequent GIF completed normally. Camera-denied recovery remained visible and imports worked; no real camera permission was accepted.

The first consolidated P6 check passed TypeScript and ESLint but found a stale P0 surface-budget expectation after adding `quad-story`. The correct known-surface arithmetic is now 17,273,840 bytes for the lab workload, excluding native/GPU/codec memory. After the correction, **474/474 tests**, whole TypeScript and ESLint passed. That run precedes the final next-round and motion busy fixes; their focused 26-test run and TypeScript/lint passed separately. A fresh final consolidation and production build remain pending alongside ongoing cloud integration.

## Cloud/event foundations

The later consolidated checkpoint passed **518/518 tests**, whole TypeScript and full ESLint, followed by the production build on patched Next.js 16.3.6 and Sharp 0.35.4. Legacy-partial handling subsequently passed nine focused tests and the disposable challenge SQL suite; final file-picker styling passed focused lint. The build includes both changes. Production routes and V2 icon assets served successfully; both lab routes returned 404 and cloud project endpoints remained disabled with 503. This is an intermediate P6 checkpoint, before cloud client and challenge screen integration.

The PNG-decoration picker also received a fresh rendered Tab check: its focus outline stays on the visible label beside the frame preview. The four corrected wrapping labels now contain their hidden file input rather than moving focus to an unrelated page position.

The isolated cloud project schema and server adapter are documented in [P6 scope contract](P6_SCOPE_CONTRACT.md). Private membership, conservative upload reservations, immutable media gates and bounded cleanup were tested in disposable PostgreSQL. Challenge protection remains fail-closed until its dedicated schema is installed. No hosted SQL, API activation or provider Storage acceptance is implied.

The event foundation is independently documented in [P7 database contract](P7_DATABASE_CONTRACT.md). It is not a completed guest, host or event UI workflow.

## Cloud library and browser recovery

`/projects/cloud` now provides the optional account library, capability/unavailable state, paginated projects and invitations, private/shared project creation, invitation IDs, membership controls, original previews/downloads, explicit upload preparation, verification status, recovery and deletion controls. The device library links to it without uploading local projects automatically. Account changes close the client/journal and remove the workspace; previews and the single selected File are released on exit. Successful sign-out with removal of local copies also clears that account's upload journal.

The browser client and journal contracts are in [P6 cloud client](P6_CLOUD_CLIENT.md). The challenge HTTP and browser client are implemented separately in [HTTP](P6_CHALLENGE_HTTP.md) and [client](P6_CHALLENGE_CLIENT.md); challenge screens are still pending. An earlier consolidated run passed **546/546 tests**, whole TypeScript and full ESLint. The later **600/600-test** checkpoint includes activity, challenge discovery/render and cleanup-isolation tests. After the final UI refinements, full ESLint and the production build, including TypeScript, passed.

Native cloud UI review used `/v2-lab/cloud`, an explicitly synthetic, temporary in-memory transport with the actual UI, browser client, upload manager and an isolated IndexedDB journal. It made no authenticated or provider requests. A 400 × 1000, 15.2 KiB synthetic PNG was chosen through the normal file picker, prepared locally, then explicitly uploaded. Held verification returned pending without exposing another ready original. Stop waiting preserved tracking. Unmount/remount restored the pending record without another upload; releasing verification and checking status produced the second available original and a digest-checked preview. An injected failed refresh displayed recovery copy, and retry cleared it. Switching to the second synthetic account removed the first account's project view and produced its separate library. A pending invitation was accepted into the empty two-person project. Private project creation also succeeded.

All eleven native IndexedDB checks passed: close/reopen stable IDs, pruning only ready tracking at the 64-record ceiling, refusing to evict uncertain records, cleanup draining existing writers, empty reopen after account cleanup and preservation of another account's records/connections. A separate module worker then opened both accounts: main-context cleanup fenced that worker's stale reads and writes, preserved its other-owner handle, and a full reopen proved no removed-owner resurrection and retained the other-owner write. Temporary worker/database cleanup completed. These checks do not establish hosted Storage, production SQL or cross-device behaviour.

Desktop and 390 × 844 light/dark views showed contained content, readable metadata and the shared themed dropdown. Review corrected tiny files displaying as zero MiB, the same-file chooser's format filter, and keyboard focus on entering/cancelling the new-project form and opening a project. Fresh native checks focused the project name, restored the creation trigger on Cancel, focused the selected project's heading, and kept the narrow dropdown inside the viewport with Escape returning to its trigger. Deleting the uploaded synthetic original returned honest pending-cleanup copy; existing signed links and downloaded copies are not claimed to be recalled. Original/project deletion confirmations focused their action and restored their trigger on cancellation. Deleting an empty disposable project returned to the focused library heading and removed its list entry.

A two-account friendship rehearsal created a shared project, invited the second synthetic ID, accepted from that account's library, and explicitly uploaded a synthetic strip. The project owner could preview the shared original but had no unsupported individual-delete control for it. Removal cleared the loaded preview, retained the documented ordinary original, prevented reinvitation and removed the project from the former member's library. Invitation-ID copy feedback and desktop dark/narrow light layouts were inspected. No external message was sent.

Independent review found and fixed three recovery defects: unfinished journals became unreachable after project access disappeared; member mutations had a separate action lock; and a room/editor cleanup failure could skip unrelated stores. The library now provides account-level tracking with explicit local-only dismissal, even while cloud access is unavailable. A pending synthetic upload survived project deletion there; reopening the deleted project failed safely, then local dismissal removed its tracking and focused the recovery heading. Membership mutations now share the detail action lock and abort controller; native inspection observed Refresh, Choose photo and Delete project all disabled during a membership request. Five cleanup tests prove room/editor failures do not skip templates or upload-journal cleanup, while editor drain remains required before project deletion. A fresh independent read-only review found the account-level recovery and shared-lock defects resolved.

The final local production build passed, with full ESLint and TypeScript. Loopback production smoke returned 200 for home, booth, device/cloud projects, templates/designer and timeline; all three lab routes returned 404. Project, challenge and memory POST endpoints returned private/no-store 503 while their flags remained disabled. All six V2 icon URLs returned 200; the manifest references the versioned 192/512/maskable assets and the Sharp 0.35.4 brand reproducibility check passed. The compiled signed-out cloud page was also inspected in the native browser. These are local production-mode checks, not a deployment, installed-device result or hosted acceptance.

## Activity and challenge integration foundations

Forward migration `006_v2_activity.sql` adds source-derived activity, personal chapters/occasion labels and explicit own UTC-month counters without replacing existing lifecycle functions. Disposable SQL checks cover compaction/replay, purge/delete history, exact-couple and project access, concealed challenge originals, CAS conflicts and real quota races. Eight simultaneous legacy saves still succeeded with all annotation/chapter slots occupied. Historical unverified archive references remain `unknown`. The strict store, same-origin `/api/memories` endpoint and account-fenced browser client passed 22 focused tests. These are source/API foundations, not a completed timeline or annual recap. See [activity contract](P6_ACTIVITY_CONTRACT.md) and [HTTP/client contract](P6_ACTIVITY_HTTP.md).

Forward migration `007_v2_challenge_discovery.sql` adds participant-only, bounded project discovery without exposing recipes, captions or sources. Actual SQL grant/scope/pagination checks and focused browser/HTTP/store tests passed. The challenge PNG adapter passed 11 focused tests covering source mapping, integrity, current authorisation, resource bounds and native-work cancellation accounting. It omits unknown capture dates with an explicit warning and cannot export a partial without an authorised partial source/geometry projection. Native challenge rendering/screens remain pending. See [discovery](P6_CHALLENGE_DISCOVERY.md) and [render contract](P6_CHALLENGE_RENDER.md).

The consolidated `supabase-setup.sql` remains at its P1 boundary. The new numbered migrations are forward source artefacts; apply/review them in order only as part of an authorised, matching integration. Replaying an older migration alone can replace a later protection hook. No hosted SQL or feature flag was changed.

## Remaining P6 work

The local then-and-now and primary/partial challenge workflows are integrated; final all-page/device acceptance remains pending. Challenge camera capture, durable creation drafts, cloud editable manifests, retained activity/chapters/recap, ritual UI and voice attachments still require integration and lifecycle review. Phone camera, microphone, native delivery, hosted authentication and external provider checks remain separate. Optional duo motion remains capability-gated and unclaimed.

## Primary challenge UI and the next memory foundations

The next local checkpoint passed 626/626 tests, whole TypeScript/ESLint and a production build. [Primary challenge UI evidence](P6_CHALLENGE_UI.md) records the actual two-account browser flow, authoritative recovery after local tracking removal, failed-submission retry, native 1072 × 3044 PNG preview and withdrawal. It explicitly separates PNG preparation from unverified OS download delivery. Partial consent screens, challenge camera capture and durable creation drafts are still required.

Forward migration 008 adds authorised partial summary/detail projection and own-ready challenge upload discovery. Its disposable SQL tests include actual reserved/finalised metadata, pending secrecy, exact digest and contributor gates, excluded withdrawal, source deletion, pagination, legacy history, revocation and reruns. Ordinary draft and cancelled-unopened discovery return an authorised empty list. The shared renderer now supports the precise partial projection without requiring a still-valid full roster; 16 renderer tests pass. Native partial export remains pending. See [partial contracts](P6_PARTIAL_RESULTS.md) and [render contract](P6_CHALLENGE_RENDER.md).

Forward migration 009 and the ritual contract/store extend the existing photo dates with explicit civil schedules, revisions, exact-couple/self-only channel consent and pause/resume/edit/delete boundaries. Twenty recurrence/store tests and disposable SQL concurrency, rerun and direct-write checks passed. Versioned rows remain legacy `active=false` and advertise `deliveryVersion:0`: worker, HTTP, UI and provider integration are not activated. See [ritual contract](P6_RITUAL_CONTRACT.md). Consolidated setup and all hosted flags remain unchanged.

## Partial results, direct entry and ritual delivery

The next integrated check passed **671/671 tests**, whole TypeScript and ESLint. This is an intermediate source checkpoint while the camera tranche proceeds. Three focused partial-fixture tests also pass after adding a stale-roster recovery case. The production build passed with the direct challenge route, TypeScript and prerendering.

[Partial UI evidence](P6_PARTIAL_UI.md) records native proposal failure/retry, excluded-proposer secrecy, explicit consent/reveal, a 1072 × 3044 partial PNG with blank positions, valid partial access after excluded withdrawal, and removal of preview/export after consent withdrawal. Desktop/narrow and light/dark checks passed locally. Source review corrections cover frozen invalid retries, submission confirmation and interrupted loading. OS download delivery and final English copy remain unverified.

[Direct challenge entry](P6_CHALLENGE_ENTRY.md) adds the planned `/challenges/[id]` route and shared account runtime. Sixteen focused tests cover access, invitations and cleanup. Development HTTP returned 200 for a valid identifier and 404 for an invalid one. Native unavailable-state review at desktop/narrow sizes and the recovery link passed with feature flags unchanged. Live authentication remains pending.

[Ritual HTTP/client](P6_RITUAL_HTTP.md) preserves the activity API and adds eight exact-couple schedule operations. Forty focused tests cover real client/handler shapes, one rate charge, revisions, response limits and account changes. [Ritual delivery](P6_RITUAL_DELIVERY.md) extends the existing scheduler through forward migration 010. Thirty-three focused tests and expanded disposable PostgreSQL checks passed, including concurrent claims/pause, recipient separation, five attempts, vanished subscriptions after ambiguous dispatch, compaction and populated reruns. Independent review corrected terminal email uncertainty, target-loop time budgets and ambiguous removed subscriptions. No email, push, hosted schema or flag was changed. Ritual UI and live provider acceptance remain pending.

Final local production smoke: valid `/challenges/[id]` returned 200 with the unavailable state, malformed ID returned 404, and `/projects/cloud` returned 200. All three development lab routes returned 404. Project, challenge and memory POST endpoints remained `no-store, private` 503 with enabled false. All six revisioned icon assets returned 200 with expected MIME types; the manifest retained standard 192/512 and maskable 512 entries. The temporary 3006 production server was stopped. The 3005 development library was left available, with synthetic cloud rehearsal stopped/cleared and viewport reset.

## Durable camera originals, retained reads and ritual UI

The next full source checkpoint passed **715/715 tests**, whole TypeScript and ESLint. [Challenge camera evidence](P6_CHALLENGE_CAMERA.md) covers durable P2 originals before selection, exact hash-checked recovery, retained retakes, bounded encoding and account/sign-out drain races. Native camera and fresh IndexedDB integration remain pending.

[Retained-strip reads](P6_RETAINED_STRIP_READS.md) add the private 011 resolver and browser client. Twenty-nine focused tests and disposable SQL checks cover exact owner/original-couple authority, no re-pair inheritance, verified archive proof, final access checks, late provider cancellation, byte/dimension/digest bounds and a real 12 MiB PNG. Independent review corrected a shared parser's former 10 MiB ceiling without increasing ordinary project image limits. No real provider read or hosted schema update was performed.

[Ritual UI evidence](P6_RITUAL_UI.md) records actual component/client browser checks and twelve focused editor/transport tests. The page has explicit local/UTC schedule review, legacy upgrade, calendar policies, creator and recipient controls, uncertain delivery, deletion and access-loss recovery. Native account switching preserved personal channel isolation. The temporary rehearsal was stopped after review. The real route's gated fallback was inspected without enabling features.

Cloud editable designs, durable challenge request drafts, the memory/activity/recap surface, optional voice captions and subsequent event stages remain outstanding. This checkpoint does not complete P6 or the user-required all-page/component gate.

The 715-test checkpoint also passed the production build. Fresh production smoke returned 200 for rituals/cloud projects, 404 for the ritual lab, private/no-store 503 for the disabled retained read, and 200 with correct types for all six V2 icon assets. The temporary production server was stopped. Subsequent [chronological browsing](P6_ACTIVITY_BROWSE.md) passed 33 focused regressions and a separate disposable SQL harness; it is newer than this full checkpoint and its memory UI is still in development.

## English-only scope update, 23/09/2026

Danny removed Vietnamese support. The Together switcher, translated interface branches and story translation catalogue were removed; all twelve decks keep their IDs, four English prompts and relaxed alternatives. Saved story plans keep the same schema and seeds. Seven story regressions, whole TypeScript and scoped ESLint passed. Native Together inspection at 1280 x 720 and 390 x 844 confirmed the English-only page, keyboard story selection, next-pose and relaxed-pose controls. The narrow document width was 385 pixels for a 390-pixel viewport, with no horizontal overflow. Final all-page review remains pending.
